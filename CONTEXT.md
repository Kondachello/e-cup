# Контекст: секвенсная ветка (задача №5)

Файл для передачи контекста в новую сессию. Положить в корень репозитория.

## Кто я в команде

№5 — секвенсные модели на GPU (RTX 2060, 6 ГБ, 16 ГБ RAM, Windows).
№1 Александр — сборка, зондирование лидерборда, финальные решения. Сабмиты
распределяет он, самому не тратить.

Соревнование E-CUP 2026, задача 3: предсказать суммарный GMV 250000 юзеров за
14.02–15.03.2026. Метрика RMSLE. Лучший результат команды 1.6489, топ-1 1.6459.

## Мои файлы

`work/scripts/seq/` — отдельная ветка, не пересекается с чужими:

| файл | что делает |
|---|---|
| `build_tensor.py` | parquet → плотный тензор `[250000, 409, 10]` fp16 |
| `fix_gmv.py` | чинит gmv в fp32 (в fp16 переполняется одна строка из 30.6 млн) |
| `make_valid3.py` | маска якорей по правилу трёх блоков |
| `train_tcn.py` | TCN и трансформер на сырых последовательностях |
| `avg_seeds.py` | усреднение сидов в лог-пространстве |
| `sweep.py`, `analyze_sweep.py` | подбор гиперпараметров |
| `add_direction.py` | добавляет модель в готовый сабмит, не размывая поправки |

Данные в `tensor/` (2.6 ГБ, в gitignore). Предсказания в `work/preds/`.

## Состояние

Усреднённый трансформер трёх сидов, январское окно, 250k, gap-30:

```
скор 1.67264   r с блендом 0.99424   ЗАПАС +0.00205
```

Рекорд проекта по запасу был 0.00193. Прирост к блендy из 19 моделей −0.00067.
Это единственная модель вне линейной оболочки бленда.

Лучшая конфигурация (из суточного свипа, 90 испытаний):
`--lr 7e-4 --channels 192 --layers 4 --heads 4 --dropout 0.0 --wd 0.0122
--aux 0.25 --batch 512 --ema 0.995 --min-anchor 30`

## Две правки, ещё не прогнанные на полном масштабе

**Ранняя остановка по калиброванному скору.** Была по сырому — та же ошибка,
что нашёл Александр. Флаг `--es-metric cal`, включён по умолчанию. По его
замерам 0.0028 на сид и разброс между сидами падает в восемь раз.

**Когорта обучения по трём блокам.** Юниверс отобран как активный в каждом из
трёх 30-дневных блоков перед тестом — проверено, все 250000 удовлетворяют. Наш
фильтр требовал одного блока и пускал на 10–13% больше слабых юзеров, среди
которых вдвое больше молчащих. Правило применяется только к обучающим якорям,
валидация и выгрузка остаются на всех 250000. Запустить `make_valid3.py` один
раз, дальше работает по умолчанию, откат флагом `--cohort1`.

## Что закрыто измерениями. Не повторять

* **Гиперпараметры сети** — суточный свип, 90 испытаний: разброс 0.004 при шуме
  прогона 0.001. Ни один параметр не значим.
* **Размер модели** — 0.9 и 2.0 млн параметров дают одинаково.
* **Длительность обучения** — вчетверо больше шагов дало +0.0004, то есть ноль.
* **Диапазон якорей** — `min_anchor` 30/60/120/200 неразличимы.
* **Больше сидов** — корреляция между сидами 0.998, усреднение трёх дало −0.0017.
* **TCN** — вес 0.079 в ансамбле против трансформера, час GPU не окупает.
* **Стекинг** — трансформер проигрывает лучшей табличной модели во всех пяти
  сегментах по давности покупки, перераспределять веса по юзерам нечего.
* **Сырой недельный ряд признаками** — 1.6789 против 1.6796, ноль.
* **Календарная поправка по 2025 году** — мой расчёт дал +0.014, но он был
  неверен: сравнивались когорты разного размера. Правильный парный расчёт
  Александра даёт +0.188. Поправку меряют зондами лидерборда, не локально.

## Правила, за которые заплачено

1. Обучение только с зазором 30 дней между обучающими якорями и валидационным.
   Без него скор завышается на 0.05–0.1.
2. Сравнивать модели только ПОСЛЕ калибровки: `calibrate.py --pred ИМЯ --bins 24`,
   затем `err_corr.py ИМЯ_cal`, именно в таком порядке.
3. Валидационное окно слепо в левом хвосте — молчащих там нет по построению.
   Отрицательные результаты в области низких прогнозов проверять на зеркальном
   якоре 2025-10-15 (`--val-anchor 287`).
4. Вклад модели в бленд равен примерно `7.1 · запас²`, где
   `запас = скор_бленда/скор_модели − корреляция_ошибок`. Корреляция сама по себе
   ничего не значит. Модель, чей прогноз есть функция 203 табличных признаков
   команды, лежит в оболочке бленда и даёт ровно ноль.
5. Выгружаемые предсказания сырые, без поправки +0.17. Смешивать до наложения
   поправок, не после.

## Индексы дней (от 2025-01-01)

287 = 2025-10-15 (зеркальный якорь), 348 = 2025-12-15, 364 = 2025-12-31
(второе окно отбора), 378 = 2026-01-14 (основная валидация), 408 = 2026-02-13
(тест). `--val-anchor` автоматически ставит границу обучающих на `якорь − 30`.

## Следующее

Прогнать три сида с новыми правками, сдать Александру `tfm3_val.parquet` и
`tfm3_test.parquet` плюс вывод `err_corr.py`.

Исследовательская часть — самообучение на неразмеченных последовательностях.
В плане отмечено как «не окупается», но это была оценка под их железо, а не
измерение. Постановка: предобучить энкодер на восстановлении замаскированных
дней истории, затем дообучить на GMV. В предобучении можно использовать февраль
2026 — входные данные теста есть, утечки нет, таргет не трогаем. Смысл в том,
что такое представление не является функцией их 203 признаков, а именно этого
план и просит искать.
