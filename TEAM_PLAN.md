# План команды: пять направлений до топ-1 (ревизия после закрытия LB-трека)

Позиция: **1.649218** (public). Топ-1: 1.64618. Разрыв: **0.0026**. Цель: <1.645.

## Что изменилось с первой версии плана

**Измерительный трек (направление 1) выполнен и закрыт.** За три дня он дал 2/3 всего
прогресса (1.6761 → 1.6492), но теперь исчерпан — измерено всё измеримое:

| Измерение | Результат | Вердикт |
|---|---|---|
| Глобальный сдвиг теста | +0.116, применён | ✓ снято |
| Смещения по сегментам GMV | −0.073/−0.033/+0.055/+0.031, применены | ✓ снято |
| Направления всех семейств моделей | по 0.0006–0.0012, применены | ✓ снято |
| Все 54 контраста моделей разом | 0.00002 | пусто |
| Оптимальный наклон (аффинная ось) | k=1.0065 ≈ 1 | пусто |
| Сезонность по сегментам | 0.00003 | пусто |

Побочный продукт: известны фундаментальные константы public-теста
(mean(t)=2.3275, mean(t²)=10.79) — скор любого нового файла прогнозируется до
четвёртого знака ДО отправки. 12 предсказаний подряд сошлись с фактом.

**Вывод жёстче прежнего: оставшиеся 0.0026 добываются ТОЛЬКО новой информацией
в моделях.** Никакая комбинация и калибровка существующего их не даст — это
не гипотеза, это сумма измерений выше.

## Проверено этой ночью (не повторять)

- **Канальная декомпозиция РАБОТАЕТ**: gmv = Поиск + Каталог, две модели, сумма.
  Сырой скор 1.6872 — лучший бустинг проекта (прямой аналог: 1.6927). Направление
  живое — развивать (человек 2).
- **Renewal/hazard-признаки (tsb_p, overdue_ratio, p_renewal) НЕ работают** как
  признаки для сильных моделей, хотя их одиночный AUC (0.832) бьёт лучший
  существующий разделитель. Деревья и сети уже реконструируют этот сигнал.
- Слабые намеренно-непохожие модели (ранговая, поведенческая, короткоокошечная):
  новизна есть, вклада нет (≤0.0001). Непохожесть ≠ полезность.

## Направления (обновлённые)

### Человек 1 (координатор): интеграция и финал
Измерения закрыты — фокус смещается:
- Встраивание каждого нового командного файла в микс (веса считаются из скоров
  без траты попыток; бюджет проб — на замеры HOB direction новых моделей).
- **Финальный репозиторий и питч** (жюри проверяет топ-15 ДО питча): README
  пайплайна, фикс зависимостей, воспроизводимость, приватность репо до конца.
- **Вопросы организаторам** (не сделано, критично): внешние данные для З3;
  число финальных сабмитов; сплит public/private; легальность измерительных
  сабмитов.
- Два финалиста: консервативный (усадки поправок под private) + агрессивный.

### Человек 2: декомпозиции таргета — ГЛАВНАЯ СТАВКА, подтверждена результатом
Канальная уже дала лучший бустинг (готовый код: `train_channel.py`). Развитие:
- Канальная v2: раздельные конфиги на канал (Каталог покупают 8% — там свой
  режим: глубже деревья, меньше min_data; Поиск — 55%), калибровка каждого
  канала отдельно ДО суммы, сетка Йенсен-поправки.
- **Недельные под-горизонты** (дни 1–7/8–14/15–21/22–30, сумма в линейном
  пространстве) — код таргетов по образцу `build_channel_targets.py`.
- **Количество × чек**: Poisson/Tweedie на число заказов × регрессия чека.
  Важно (из рыночной сверки): чек падает −26% г/г — не экстраполировать вверх.
- Комбинация: канал × под-горизонт (8 голов) — если первые два сработают.

### Человек 3: P(buy)-стек
Без изменений + два уточнения из ресёрча:
- Калибровку взвешивать по величине E[log1p|buy] (цена ошибки p растёт с m²).
- beta-calibration / Venn-Abers вместо isotonic на малых сегментах; focal loss
  НЕ брать (не строго proper, дисбаланс 46/54 мал).

### Человек 4: непараметрика
Без изменений (SVD user×week, kNN-траектории, Croston/TSB как ПРЕДИКТОРЫ).
Уточнение: TSB-величины как ПРИЗНАКИ уже проверены — мертво; как отдельный
слабый предиктор — не проверено, но ожидания снижены. Приоритет kNN и SVD.

### Человек 5: секвенс на GPU + дистилляция
Без изменений. Напоминание из ресёрча: перед дистилляцией бленда на тест
СНЯТЬ измеренный глобальный сдвиг из таргетов дистилляции, иначе студент
его зацементирует. Чередовать train/pseudo-label батчи с откатом по валидации.

## Интерфейс (без изменений, короткая версия)

1. Код и признаки — из репо (README → «Запуск», 15 минут).
2. Протокол: `--gap-days 30`, валидация 2026-01-14.
3. Выход: `work/preds/ИМЯ_val.parquet` + `ИМЯ_test.parquet` (exp_lib.save_preds).
4. Приёмка: `python work/scripts/err_corr.py ИМЯ` — val и корреляция ошибок.
5. Готовые предсказания всех 28 наших моделей: `work/preds_pack/` (val с таргетом
   + test, один parquet на сплит) — для локальных экспериментов без пересборки.
6. Пробы на LB — через координатора (бюджет общий, 5/день).

## Арифметика до цели (обновлённая, консервативнее)

| Источник | Оценка |
|---|---|
| Канальная v2 + под-горизонты + количество×чек (чел. 2) | 0.0008–0.0020 |
| P(buy)-стек (чел. 3) | 0.0004–0.0012 |
| kNN/SVD (чел. 4) | 0.0004–0.0012 |
| GPU-секвенс + дистилляция (чел. 5) | 0.0004–0.0012 |
| Интеграция всего через микс (чел. 1) | +30–50% к сумме |

Середины ≈ 0.0028–0.0030 при нужных 0.0026. Впритык — поэтому нужны все четыре
модельных направления, а не одно.
