# ПЕЧАТЬ ПРОГНОЗА: F13_g0 (γ=0), запечатано ДО замера

Дата: 30.08.2026, ~16:00 МСК. Файл: `submissions/F13_g0.csv`,
sha256 `ebf7883a46ed6ecee3eca8080ff4d9f9e1180c65b6e34a99887f7c64c12d7626`.

Протокол кампании: прогноз паблика печатается в git **до** отправки, галка ставится
только при попадании замера в коридор. Этот документ — та самая печать.

## 0. Блокер сборки снят не сборкой, а находкой

`TEAM_TODO_3008` §0-ЧЕТЫРЕ ставил задачу восстановить 14 направлений
(seg_cinnab–seg_amber, seg_lapis–seg_gneiss), которых нет в `gls_draft/directions/`, и собрать из них γ=0.
**Восстанавливать не нужно: файл уже был посчитан 29.08** и лежит вектором lp в
скретчпаде той сессии — `F8_final_lp_a3g0.npy` (путь записан в самом рецепте,
поле `lp_path` в `k1b_final_a3g0.json`).

Подлинность вектора доказана двумя независимыми сверками:

| проверка | результат |
|---|---|
| `F8_final_lp_f12.npy` против отправленного и **замеренного** `F12_ebint.csv` | max\|Δlp\| = 1.11e-16 (машинная точность) |
| `F8_final_lp.npy` против `F8_priv.csv`, пересборка новым скриптом | **побайтовое совпадение**, sha256 `5eaaef9f…` = реестр гарда |
| моменты `a3g0` против `k1b_final_a3g0.json` | mean 2.330895119958, sd 1.632157077760, клипов 4 — все три до 12-го знака |

То есть скретчпад-векторы — ровно те объекты, из которых собирались отправленные
файлы, а не их реконструкция. Инструмент сборки — `work/scripts/assemble_from_lp.py`,
у него есть режим `--verify`, и на F8 он зелёный побайтово.

Побочный урок для доктрины: сборщики кампании держали финальный lp во временном
каталоге сессии, а в git клали только CSV. Пока сессия жива, это незаметно; после
неё файл невоспроизводим. `assemble_from_lp.py` закрывает разрыв, но правило
сильнее инструмента: **lp финального кандидата класть в репозиторий вместе с CSV.**

## 1. Что за файл

γ = 0 — отказ от усадки доз, полное доверие замеренным κ. База F7_priv,
те же 46 осей, дозы `d_fin` из `gls_state_a3g0.npz` (тождество проверено:
`d_fin` — приращение к F7, `doses_F7 + d_fin = doses_F8`, обе сверки дали 0.00e+00).

| | F8 | F12 | **F13_g0** |
|---|---|---|---|
| Σ\|дозы\| по 16 модельным осям | 2.8965 | 3.4712 | **5.7612** |
| сырая паблик-алгебра | 1.6457652259 | 1.6456258159 | **1.6456097936** |
| факт с платформы | 1.6458057389 | 1.6456761696 | — |
| промах алгебры | +4.051e-5 | +5.035e-5 | ? |

## 2. Печать

Правило штаба восстановлено по F12 и воспроизводится точно: печать F12 =
алгебра F12 + промах F8 = 1.6456258159 + 4.051e-5 = **1.6456663**, ровно то число,
что стоит в `finalists.md §0`. Промах лёг +9.9e-6 = 0.45 шума.

У γ=0 дозы в 1.66 раза крупнее, чем у F12, поэтому постоянная модель промаха —
уже экстраполяция. Две модели расходятся, и двумя точками спор не решается:

| модель промаха | промах | печать |
|---|---|---|
| постоянная (правило, подтверждённое на F12) | +4.54e-5 | **1.6456552** |
| пропорциональная Σ\|c\| | +8.21e-5 | 1.6456919 |
| линейная по двум точкам | +8.96e-5 | 1.6456994 |

**ПЕЧАТЬ: 1.645678**, 68 % [1.645645, 1.645710], 95 % [1.645620, 1.645740].
Полоса шире, чем у F12, намеренно: она обязана покрывать обе модели промаха.

**Правило приёмки — прежнее:** галка только при
\|замер − 1.6456097936\| ≤ 1.2e-4, то есть замер ∈ [1.645490, 1.645730].
Промах выше коридора означает, что κ переоценены — тогда остаёмся на F12+F8,
потратив слот, который у дедлайна всё равно сгорал.

## 3. Чего печать НЕ утверждает

Она не утверждает, что γ=0 лучше F12 по паблику. Две модели из трёх дают
1.64569–1.64570, то есть **хуже** замеренного F12 (1.6456762) на 1.5–2.5 шума;
это сходится с оценкой штаба «ожидание хуже на 0.6 шума». Ценность γ=0 во втором
слоте — не ожидание, а **дисперсия**: в зачёте по `min(g1, g2)` независимая
добавка разброса при том же среднем и есть лотерейный билет.


---

# РЕЗУЛЬТАТ ЗАМЕРА (дописано 30.08, вечер)

**Факт с платформы: 1.6457023817.**

| | значение |
|---|---|
| печать (выше, до отправки) | 1.645678, 68 % [1.645645, 1.645710] |
| промах печати | **+2.44e-5 = 1.11 шума, внутри 68 %** |
| \|замер − алгебра\| | 9.26e-5 при коридоре 1.2e-4 — **ПРИНЯТ** |
| против F12 | +2.62e-5 = +1.19 шума хуже, ровно как предупреждала печать §3 |

## Спор о модели промаха закрыт третьей точкой

Печать честно называла, что три модели расходятся и двумя точками спор не решить.
Замер решил его — и **правило, которым штаб пользовался до сих пор, проиграло**:

| модель промаха | предсказывала | промах модели |
|---|---|---|
| постоянная (правило, применённое к F12) | 4.54e-5 | **+4.72e-5 = 2.14 шума** |
| пропорциональная Σ\|c\| | 8.21e-5 | +1.05e-5 = 0.48 шума |
| **линейная по двум точкам** | **8.96e-5** | **+3.03e-6 = 0.14 шума** |

Регрессия по всем трём замерам (Σ\|c\| по 16 модельным осям → промах алгебры):

    промах = 1.825e-5 · Σ|c| − 1.265e-5,   остатки 0.01 / 0.02 / 0.00 шума

Три точки на прямой с остатками ниже 0.02 шума — это не подгонка по двум, а закон.

**Следствие для доктрины.** Правило «печать = алгебра + промах предыдущего файла»
работает, только пока Σ\|c\| близки: у F8 (2.90) и F12 (3.47) они отличались на 20 %,
и правило попало в 0.45 шума. У γ=0 (5.76) разрыв вдвое — и постоянная модель
ошиблась бы на 2.1 шума. **Печатать впредь по линейной формуле выше**, а Σ\|c\|
считать по 16 модельным осям (`Σ|d_fin|`, помня что `d_fin` — приращение к F7).
