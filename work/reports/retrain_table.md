# Отгружено / ретрейн / Δ — измеренная воспроизводимость решения

Трек 4, 25.08. Пункт 7 `PLAN_FINAL_WEEK`. Скрипт `work/scripts/retrain_table.py`,
числа `work/reports/retrain_table.json`.

## Зачем именно так

Побитовое воспроизведение отгруженного файла недостижимо и не является правильным
обещанием: часть членов — нейросети, у одной обучение обрывалось на 4500 шаге из 11736.
Проверяемое утверждение звучит иначе: **код в репозитории порождает модели решения, а
переобучение отличается от отгруженного на измеренную величину.** Ниже эта величина.

Мерится на трёх уровнях: собственный скор члена, расхождение векторов в лог-пространстве
и — главное — сдвиг ИТОГОВОГО скора при подстановке переобученного члена на его же вес.
Веса не переподбираются: иначе Δ маскируется новой подгонкой.

## Результат

Эталон бленда 1.665647.

| член | вес | отгружено | ретрейн | Δ соло | rms векторов | **Δ бленда** |
|---|---|---|---|---|---|---|
| weak_an_d_cal | 0.0455 | 1.670760 | 1.672794 | +0.002034 | 0.1466 | **+0.000093** |
| weak_ft_recency_cal | 0.0434 | 1.697701 | 1.701313 | +0.003612 | 0.1204 | **+0.000055** |
| c_ts2_s42_cal | 0.0333 | 1.668343 | 1.668328 | −0.000015 | 0.0652 | +0.000000 |
| febspec2_cal | 0.0089 | 1.770665 | 1.770665 | 0.000000 | 0.0000 | +0.000000 |
| wklin | 0.0710 | 1.684188 | 1.684188 | 0.000000 | 0.0000 | +0.000000 |
| wklin_wk_cal | 0.0028 | 1.698418 | 1.698418 | 0.000000 | 0.0000 | +0.000000 |
| **все подставлены** | **0.2049** | 1.665647 | 1.665798 | | | **+0.000151** |

**Переобучение 20.5% веса бленда сдвигает решение на +0.000151 RMSLE** — 6.9 шумовых
единиц одного замера лидерборда.

## Главное: расхождение объясняется одной причиной

Закономерность полная, исключений нет.

| член | откуда берутся обучающие якоря | Δ бленда |
|---|---|---|
| febspec2 | СВОЙ набор по протоколу (39 недельных от когорты 2025-02-17) | 0.000000 |
| wklin, wklin_wk | заданы явно (`--anchor-source disk`) | 0.000000 |
| c_ts2_s42 | `--n-anchors 14` из каталога, каталог совпал | 0.000000 |
| weak_ft_recency | `--n-anchors 14` из каталога | +0.000055 |
| weak_an_d | **выбор 4 якорей из ПУЛА каталога** (`sel_seed=77`) | +0.000093 |

**Воспроизводится ровно то, чей набор якорей пинится протоколом. Дрейфует ровно то, чей
набор зависит от содержимого каталога.** У `weak_an_d` сырой скор разошёлся на 0.012
(1.683269 против 1.695393): на машине сборки в пуле было больше якорей, `sel_seed=77`
выбрал из него другую четвёрку — то есть обучилась другая модель, а не та же с шумом.

Это та же переменная, которой объяснились `lag_tta` (воспроизведён побитово после
возврата каталога к канону) и `wklin` (воспроизведён после восстановления набора шага 7).

## Что из этого следует

1. **Лекарство уже написано:** `anchor_plan(source="protocol")` берёт набор из
   `train_anchors(N)`, не заглядывая в каталог. Его нужно распространить на
   `train_weak.py` и на `--n-anchors` в `train_gbdt.py` — тогда дрейфующая часть
   станет воспроизводимой, и Δ по этим двум членам уйдёт в ноль.
2. **Для проверки решение честно описывать так:** переобучение по коду репозитория даёт
   бленд, отличающийся от отгруженного на 0.000151 на покрытых 20.5% веса, причём
   расхождение локализовано в двух членах с непинованным набором якорей и объяснено.
3. `provenance.py` пишет фактический набор якорей в каждый артефакт — расхождения такого
   рода впредь видны сразу, без расследования.

## Чего в таблице нет и почему

| члены | вес | причина |
|---|---|---|
| fusion_v3c_avg, fusion_v3ctl | 0.335 | нужны тензоры `work/seq3`, их нет на этой машине |
| kostya46, gseq_small, gseq_big, fusion_f | 0.390 | внешние: обучены на машинах Кости и GPU-трека |
| behavonly_avg | 0.041 | два из трёх сидов обучены на всех 27 якорях каталога сборки |
| lagd28 | 0.035 | отгружен по протоколу `n18` (регенерация №1); мой оригинал `n9` воспроизведён побитово 24.08 |

Покрытие ограничено машиной, а не методом: команда та же, скрипт тот же.

---

# Ретрейн заморозки (28.08): состав перестроился, покрытие 7.8%

Прогон под заморозку на действующем составе (18 членов после реопта с tfm4/kevf).

| член | вес | отгружено | ретрейн | Δ соло | rms | Δ бленда |
|---|---|---|---|---|---|---|
| weak_an_d_cal | 0.0301 | 1.670760 | 1.672794 | +0.002034 | 0.147 | +0.000058 |
| weak_ft_recency_cal | 0.0267 | 1.697701 | 1.701313 | +0.003612 | 0.120 | +0.000032 |
| lagd28 | 0.0202 | 1.744574 | 1.745023 | +0.000448 | 0.131 | −0.000011 |
| febspec2_cal | 0.0011 | 1.770665 | 1.770665 | 0.000000 | **0.0000** | +0.000000 |
| c_ts2_s42_cal | 0.0000 | 1.668343 | 1.668328 | −0.000015 | 0.065 | +0.000000 |
| **итого** | **0.078** | 1.665647 | 1.665727 | | | **+0.000080** |

**Переобучение 7.8% веса сдвигает решение на +0.000080** — 3.6 шума одного замера.

## Три вещи, которые надо читать вместе с таблицей

**1. Покрытие упало не потому, что стало хуже.** Было 20.5%, стало 7.8% — состав
перестроился: GPU-секвенсы (`tfm4_cal` 0.201, `kevf_s42_cal` 0.143, `tfm4_tabless_cal`
0.102) забрали почти половину веса, `kostya46` упал с 0.246 до 0.088. Табличные модели,
которые эта машина умеет переобучать, теперь весят мало. Метод тот же, доля другая.

**2. Все четыре табличные воспроизвели САМИ СЕБЯ до знака.** Скоры прогонов 25.08 и
28.08 совпали полностью (1.695393 / 1.730688 / 1.696260 / 1.785259), хотя между ними
набор якорей был отвязан от каталога. То есть пины поведение не изменили — ровно это и
проверялось.

**3. Расхождение `lagd28` — РАЗНИЦА ПРОТОКОЛОВ, а не невоспроизводимость.** Отгруженный
файл собран регенерацией №1 (`n18 it=574`, скор 1.744574), мой прогон идёт по исходному
протоколу трека 4 (`n9 it=655`, 1.745023). Обе цифры записаны в `scores.tsv` как разные
строки. Мой прогон воспроизвёл СВОЙ оригинал побитово ещё 24.08 (rms 1.5e-17). Сравнивать
надо с записью, отвечающей запуску, а не с файлом под тем же именем.

## Что осталось непокрытым

`tfm4`, `kevf`, `gseq`, `fusion` (суммарно ~0.66) обучаются на GPU-машинах треков №3 и
№5; их провенанс закрывается кодом и командами в репозитории, а не переобучением здесь.
