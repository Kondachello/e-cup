# A2. MB409: посылку тратить НЕ на это. Разбор с отменой моего же пункта

Саша, 30.08 ~20:10. Часть Оли, пункт A2 из `HANDOFF_olya_3008_evening.md`.
**Вывод отменяет пункт №1 плана посылок в `TEAM_TODO_3008_EVENING.md`.**

## 1. Что я поставил в план и на каком основании

`finalists.md §П2` говорит: исправленные признаки тестового якоря собраны
(`anchor=2026-02-13.mb409.*`, три тира), сдвиг замерен (по покрытым 0.093 веса
среднее −0.000744, sd 0.003719, у 2.26 % юзеров > 0.01), остаток **осознанно не
выкуплен**. Читается как «известная поправка, которую отложили из-за дефицита слотов».
Сегодня слоты сгорают — значит выкупать. Так я и записал.

## 2. Почему это неверно: ось уже измерена и на три четверти взята

Более ранняя запись, которую §П2 не пересказывает — `work/reports/probes_5.md`,
пять ортогональных проб 20.08. **Обрезка истории — это проба mdl_halite, и она была
поставлена на лидерборде:**

```
проба            скор            k        сигм   вклад после усадки
mdl_halite обрезка     1.6478627751    +0.756     2.3      0.000110
```

Оттуда же, дословный вердикт §«находка 2»: дефект обрезки выкупается прямым шагом
**без переобучения**, три четверти направления взяты одной попыткой, и кампания по
пересборке признаков ради этого **не нужна**.

Строка 89 того же отчёта закрывает и сам ретрейн: «честная цена 0.000381 ± 0.000059
на валидации **не переносится на тест** либо приведение к моментам её съедает —
задание на ретрейн MAX_BACK 409 можно снять с очереди».

## 3. Решение 28.08 было принято со знанием этого

`work/handoff/REPLY_olya_2808.md §5`: «остаток ~2 шума; слоты дефицитны и
зарезервированы под F5 и kevf3; три четверти цены выкуплены пробой mdl_halite.
Зафиксируй как невыкупленный остаток, осознанно».

То есть в §П2 записан **остаток от уже применённой оси**, а не неиспользованная
поправка. Мой план прочитал §П2 без §5 и без `probes_5.md`.

## 4. Что осталось законно открытым — и это НЕ пересборка

Открыт **передоз/недодоз по уже замеренной оси mdl_halite**: применённая доза против κ̂ = 0.756.
Это `redose_multiplier` из `work/doctrine/dose.py`, прибавка `q·(b_opt − b_now)²/(2F0)`,
переобучения не требует, признаки не нужны.

**Но прежде чем тратить на это посылку — обязателен `axis_applied.py`.** Причина ровно
та, на которой мы 30.08 чуть не спалили две посылки из пяти (0-БИС): F8 и F12 — это
совместные солвы по 46 осям, и если ось mdl_halite входит в эти 46, солвер **уже перерешал**
её дозу. Тогда «остаток ~2 шума» от 28.08 частично или полностью съеден, а
`joint_gain`/старая запись этого не видят.

## 5. Почему проверку нельзя сделать в этом клоне

Направление mdl_halite берётся из `work/reports/mb_fix_preds.npz` (суррогатный бленд из 7
моделей на двух плечах MAX_BACK 379/409, строится `mb_fix_infer.py`). В клоне
`e-cup` этого файла нет, как нет `work/features/`, `work/preds/` и
`work/reports/gls_draft/`. Проверено: `FEATURES_DIR = work/features` (`common.py:18`)
отсутствует, `anchor=2026-02-13.mb409.*` на диске нет.

Значит:
* пересборка на исправленных признаках здесь невозможна — и, по §2, не нужна;
* проверка передоза mdl_halite возможна **только там, где лежит `mb_fix_preds.npz`** — это
  машина с полным рабочим деревом (у Оли либо в исходном рабочем клоне).

## 6. Что делать с этим сегодня

1. **Из плана посылок пункт №1 снять.** Пересборка MB409 не покупает ничего, что не
   было бы взято 20.08, и её собственный отчёт снимает ретрейн с очереди.
2. Если у Оли на машине есть `mb_fix_preds.npz` — один прогон, десять минут:
   `axis_applied.py --dir <направление mdl_halite> --file F12_ebint`. `proj ≈ 1` — ось
   отработана, тема закрыта окончательно. `proj` заметно меньше 1 при живом `q` —
   тогда это redose, и вот на него посылка оправдана.
3. Освободившуюся посылку отдать следующему в очереди.

## 7. Оговорка

Я не измерял здесь ничего нового: это разбор записей кампании. Числа взяты из
`probes_5.md` (строки 73, 89, 103, 162–170), `REPLY_olya_2808.md §5`,
`finalists.md §П2` и `KNOWLEDGE.md` (таблица пяти проб). Если у кого-то есть замер,
противоречащий §2, — он старше моего разбора, и решает он.
