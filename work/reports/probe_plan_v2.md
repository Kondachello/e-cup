# Probe plan v2 — 5 попыток на 2026-08-19 (math-only, по замерам дня 1)

Все числа ниже пересчитаны из измеренных скоров и сохранённых файлов; probe-векторы
записаны в `work/probes/*.npy` (детерминированы, пересборка не нужна). Базовый скор
f_A8(прогноз) = **1.652269**; формулы — как в probe_design.md §1.

## 0. Верификация дня 1 — всё сходится

- m̂(e_A1 по PA_gmv) = (−0.07295, −0.03263, +0.05467; S4 implied +0.03114); S4: 0.03010 — с shrink).
- Feb-проба: q_feb = 0.56989 (локально), c_feb = mean(e_A5·d_feb) = **−0.034928**,
  δ* = 0.061289;

## 1.

Шаг вдоль d_feb не ортогонален PA_gmv → он ЗАНОВО внёс сегментные средние:


Все эти функционалы уже измерены (пробы дня 1 + локальные средние d_feb) → joint solve
по блоку {const, 1S1, 1S2, 1S3, d_feb}:

    β_F0 = (+0.0206, −0.0680, −0.0351, +0.0272, −0.0262)
    imp f² = 0.000919, noise inflation = 0.000011 → честный gain = +0.000275
    f(F0) = 1.651991, min lp = 0.057 (без клипа)

F0-блок входит в любой завтрашний файл бесплатно. Полный re-fit по span 7 файлов
ОТВЕРГНУТ: система ill-conditioned, raw imp 0.0095 при noise inflation 0.0075 — шум
(min eig после центрирования мал; e_A8 и так ⊥ файловому span в пределах точности).

## 2. Теория: чем платим и что покупаем (вопросы 3–4)

Одна попытка = один линейный функционал c_h = mean_P(e_A8·h) для ЛЮБОГО локального h
(файл пробы = expm1(lp_A8 + s·h)).

**Прямой сабмит нового файла X ≡ направленная проба с h = lp_X−lp_A8, s=1** — тот же один
функционал (c = (f_X²−f_A8²−q)/2). Различия только практические: прямой сабмит даёт точный
f_X (файл-кандидат на финальный отбор, якорь Грама), проба малой амплитуды — чуть меньший
шумовой член s·σ(q_P)/2 и скор попытки возле лучшего. Правило:
- новые МОДЕЛИ с новыми лоссами (fusion/mlpbin/ziln) → собрать на val в my27 и сабмитить
  НАПРЯМУЮ (внутренние веса доверяем val, снаружи нужен один функционал + точный f);
- перевзвешивание УЖЕ ГОТОВЫХ компонент → направленные пробы, ориентированные в сторону
  val-оптимума (тогда проба сама — кандидат-улучшение, см. §4).

**Геометрия новизны** (сколько информации осталось в направлении после проекции на уже
измеренный span {7 файлов, const, 3 сегмента, d_feb}, посчитано на тесте):

| кандидат | q=mean(d²) | новизна q⊥/q |
|---|---|---|
| my26 | 0.0512 | **3%** — проба почти бесполезна |
| caruana_v1 | 0.0698 | **1%** — внутри span; к тому же leak-selected (val 1.624 — фикция) |
| mlp2_big_cal | 0.0605 | 7% |
| twdeep | 0.0219 | 42% |
| c_xtw_s42 | ~0.03 | 40% |
| c_ts2_s42 | ~0.02 | 40% |
| seq2tr_f | 0.0855 | 67% |
| gru_final | 0.1240 | 75% |

## 3. Val-калибровка (только чистые файлы!)

**Лики:** blend_w1a_val (val 1.6233 vs LB 1.6755), twlog_probe_val (1.5945), caruana_v1
(1.6240) обучены/собраны без gap → любые val-оценки с их участием испорчены. Чистая база
анализа: c_cand_val (1.6717), поправленная const+PA_gmv+d_feb-резидуализацией.

**Калибровка переносимости κ = LB gain / val gain:**
- PA_gmv (день 1): val honest +0.00027 → LB +0.00068, κ ≈ 2.5; профиль val↔test
  коррелировал с ρ ≈ −0.99 (знак перевернулся — пробе всё равно, она меряет знак).
- febdir: чистый val gain +0.000084 → LB +0.000648, **κ ≈ 7.7** (сезонное val-слепое).
- Для модельных направлений консервативно κ ∈ [0.5, 2.5], центр 1.0.

**Направления, две скобки** (верхняя: резидуализация только {const,segs,d_feb};
нижняя: + clean-аналоги файлового span {c_twlog_s42, c_dirlgb_s42, mlp2_final}):

| направление | gain_val верх | gain_val низ |
|---|---|---|
| c_ts2_s42 | 0.00225 | 0.00106 |
| gru_final | 0.00136 | 0.00101 |
| c_xtw_s42 | 0.00121 | 0.00018 |
| twdeep | 0.00102 | 0.00038 |
| seq2tr_f | 0.00080 | 0.00049 |
| my26 | 0.00055 | ~0 (3% новизны) |

Кластеры (корреляции резидуализированных направлений): TREE {twdeep,xtw,ts2} 0.53–0.77,
SEQ {seq2tr_f,gru} 0.52, между кластерами 0.1–0.3. **Honest cross-fit 50/50 (5 реп):**
TREE joint = 0.00249, причём 1-D свёртка (по val-весам, амплитуда перемеряется) = 0.00246
— без потерь; SEQ = 0.00149/0.00144; joint-5 = 0.00322; нижняя скобка joint-5 = 0.00194.
→ **Двух 1-D проб (по кластеру на пробу) достаточно**, внутрикластерная форма стабильна.

**Вопрос 2 — сегментные раскрои, ортогональные gmv (на PA_gmv-исправленном остатке):**

| раскрой | Σs·m² | honest |
|---|---|---|
| rec_active 0-3/4-14/15-60+ | 0.00063 | **+0.00017** ← лучший |
| pred-deciles (чистая база; артефакт прокси устранён) | 0.00058 | ~+0.00017, в основном внутри span |
| active_days_30 | 0.00010 | +0.00002 |
| rec_order / ord_cnt_90 / searches_30 | ≤0.00017 | ≤0, шум |
| btyd_p_alive / btyd_exp_purch30 квартили | 0.00005/0.00006 | +0.000005/9 |
| ord_cnt_ya_tgt 0/1/2+ | 0.00010 | +0.00001 (val слеп к сезону — upside неизвестен) |

Вывод: ортогональные сегментные раскрои ПОЧТИ ИСЧЕРПАНЫ после раунда PA_gmv: максимум
rec_active +0.00017·κ[1–2.5] ≈ +0.0002–0.0004. Тратить на это 2–3 попытки (по сегментам)
нельзя; правильная форма — **одна профильная проба**: h = Σ_k m_val,k·1[bucket_k]
(меряет амплитуду val-профиля целиком; опыт раунда 1: профиль переносится с ρ²≈0.97).
P(buy)-корзины (btyd) пусты — не брать.

## 4. Дизайн проб (векторы в `work/probes/`, ориентированы в сторону val-улучшения)

Все h резидуализированы на тесте против полного измеренного span (7 файлов + const +
segs + d_feb, ridge 3e-4) и нормированы; проба = expm1(lp_A8 + s·h). Ожидаемое c < 0.

| вектор | состав (val-веса на ⊥-частях) | q | шаг s | скор пробы mid(κ=1) | worst(κ=0) |
|---|---|---|---|---|---|
| h_tree | −(0.203·twdeep − 0.157·xtw − 0.681·ts2)⊥, rms 0.12 | 0.01440 | 0.45 | **1.6507** | 1.6532 |
| h_seq | −(−0.094·seq2tr_f − 0.176·gru)⊥, rms 0.12 | 0.01440 | 0.40 | **1.6511** | 1.6530 |
| h_tree2 | ts2⊥ ⊖ h_tree (внутрикластерный сплит, страховка) | 0.01440 | 0.30 | 1.6527 | 1.6527 |
| h_rec | val-профиль rec_active (−0.0158/+0.0257/+0.0242), резид. | 0.000322 | 0.80 | 1.6522 | 1.6523 |

min lp по всем пробам ≥ 0.05 (клип-гард в генераторе; точность не страдает — c меряется
для фактического h_eff = lp_file − lp_A8). Точность: σ(c) ≈ 5e-5 « |c|~0.007–0.011
(включая 5-значное округление скора и гипергеометрический шум q_P). Шаги s = 0.6–0.75
от val-δ*: проба сама является кандидатом-улучшением (mid-скоры выше), а F* доводит шаг
до измеренного оптимума δ*_test = −c/q.

Извлечение (после скора пробы): **c = (f_probe² − f_A8² − s²·q)/(2s)**, f_A8 — измеренный.

## 5. Дерево решений на 5 попыток

Гейт G1: |f_A8 − 1.652269| ≤ 5e-4 → штатно. Иначе — вся дифференциальная алгебра остаётся
точной (пробы дифференцируются от ИЗМЕРЕННОГО f_A8), но F0-блок применять с shrink 0.5.

Извлечь c_tree; κ̂_tree = c_tree / (−0.00885).

Извлечь c_seq;
κ̂_seq = c_seq / (−0.00775). )

**Попытка 4 — ветвление:**
- **(A) my27 готов** (fusion/mlpbin/ziln доехали, val-бленд собран и clean):
  сабмит my27 НАПРЯМУЮ. Даёт c_my27 = (f_my27² − f_A8² − q_my27)/2 + точный f_my27
  (якорь Грама, кандидат финального отбора). Ожидаемый вклад в F*: +0.0003–0.0008. ожидание +0.0002–0.0008 в этом режиме. +0.0001–0.0006.

если прогнозный выигрыш < 0.0005 —
откат к чистому F0-файлу (+0.000275 гарантированно).

## 6. Прогноз финального скора дня по веткам

|---|---|
| null: c_tree ≈ c_seq ≈ 0 (κ=0) | 1.6518 (= F0 + ветка 4) |
| консервативно: κ=0.5, нижняя скобка | 1.6513 |
| **центрально: κ=1, середина скобок** | **1.6491** |
| горячо: κ=1.5–2, верхняя скобка | 1.6430–1.6460 |

Взвешенная оценка: **≈1.6491 (+0.0022 / −0.0035)**. Топ-1 (1.64618) в центральном
сценарии ещё не бит; бьётся в горячей ветке или после my27. До 1.639 арифметика LB не
дотягивает — мост это очередь моделей: my27 меряется в ветке (A) и встраивается тем же
Грамом в следующие дни.

## 7. Генерация (paste-run; 2 threads, без обучения)

```python
import numpy as np, polars as pl, json
ROOT = "/Users/alexanderkondakov/ozon-cup"
df = pl.read_csv(f"{ROOT}/submissions/FILE.csv", schema_overrides={"user_id": pl.Int64}).sort("user_id")
uid = df["user_id"].to_numpy()
lpA8 = np.log1p(np.clip(df["predict"].to_numpy().astype(np.float64), 0, None))
H = {n: np.load(f"{ROOT}/work/probes/{n}.npy") for n in ("h_tree","h_seq","h_tree2","h_rec")}
STEP = {"h_tree":0.45, "h_seq":0.40, "h_tree2":0.30, "h_rec":0.80}
def write(name, l):
    pl.DataFrame({"user_id": uid, "predict": np.expm1(np.clip(l, 0, None))}
                 ).write_csv(f"{ROOT}/submissions/{name}.csv")
write("FILE.csv", lpA8 + STEP["h_tree"]*H["h_tree"])
write("FILE.csv",  lpA8 + STEP["h_seq"] *H["h_seq"])
# ветка B: write("B4_probe_tree2", lpA8 + 0.30*H["h_tree2"]); ветка C: write("B4_probe_rec", lpA8 + 0.80*H["h_rec"])

# ---- после скоров: извлечение и финал ----
fA8 = FIXME_measured          # попытка 1
fB  = {"h_tree": FIXME, "h_seq": FIXME}   # + "h_tree2"/"h_rec"/my27 по ветке
c = {n: (fB[n]**2 - fA8**2 - STEP[n]**2*(H[n]**2).mean())/(2*STEP[n]) for n in fB}
# my27 (ветка A): d = lp_my27 - lpA8; c["my27"] = (f_my27**2 - fA8**2 - (d*d).mean())/2; H["my27"]=d; STEP-не нужен
# F0-блок (уже измеренные функционалы дня 1):
g = pl.read_parquet(f"{ROOT}/work/features/anchor=2026-02-13.parquet", columns=["user_id","gmv_sum_365"]).sort("user_id")
gv = g["gmv_sum_365"].to_numpy(); pos = gv > 0
q = np.quantile(gv[pos], np.linspace(0, 1, 10)[1:-1])
gd = np.zeros(len(gv), int); gd[pos] = np.searchsorted(q, gv[pos], side="right") + 1
S = {1: gd >= 8, 2: (gd >= 5) & (gd <= 7), 3: gd <= 2}
lpA5 = np.log1p(np.clip(pl.read_csv(f"{ROOT}/submissions/FILE.csv",
        schema_overrides={"user_id": pl.Int64}).sort("user_id")["predict"].to_numpy().astype(np.float64), 0, None))
d_feb = np.log1p(np.clip(pl.read_parquet(f"{ROOT}/work/preds/febspec_test.parquet"
        ).sort("user_id")["pred"].to_numpy().astype(np.float64), 0, None)) - lpA5
N = len(lpA8)
U = [np.ones(N), S[1].astype(float), S[2].astype(float), S[3].astype(float), d_feb]
cv = [0.0042044, 0.193804*-0.0332077, 0.290704*-0.0101687, 0.321688*0.0334318, 0.0]
for n in c: U.append(H[n]); cv.append(c[n])
U = np.stack(U); cv = np.array(cv); G = U @ U.T / N
beta = np.linalg.solve(G + 1e-8*np.eye(len(cv)), cv)
imp = float(cv @ beta)
print("f(FILE.csv) predicted =", np.sqrt(fA8**2 - imp))   # гейт FILE.csv: fA8 - f(FILE.csv) >= 0.0005
lp5 = lpA8 - beta @ U
assert lp5.min() > -1e-9 or print("clip:", (lp5 < 0).sum())
write("FILE.csv", lp5)
```

## 8. Риски и ограничения

- κ val→test неопределён — но пробы МЕРЯЮТ истину: неудачная проба стоит только
  альтернативную попытку, применённый по замеру шаг не может ухудшить (gain = c²/q ≥ 0
  минус σ-шум ~1e-5).
  E[Δf²] = +δ²q при неизвестном знаке c).
- my26/caruana_v1 не сабмитить и не пробовать — 1–3% новизны, caruana leak-selected.
- Сегментные вторые раунды дороже направленных проб: max EV +0.0004 за попытку против
  +0.0015–0.0025 у модельных кластеров.
- private-перенос: коррекции по public-функционалам;
- Если завтра доедут ДВЕ+ новые модели порознь (не успели в my27) — они входят в ветку (A)
  одним блендом; пробовать по отдельности только вместо ветки (C), приоритет по val-новизне.
