# Трек №5 → треку №4: результат этапа C (застоялость 28 дней)

Дополнение к `work/reports/track5_answer_to_track4.md`. Файлы в `work/handoff/`.

## Файлы

```
work/handoff/tfm3_stale28_val.parquet    якорь 350, чекпойнт фазы A (обучение по 348)
work/handoff/tfm3_stale28_test.parquet   якорь 380, чекпойнт фазы B (обучение по 378)
```

Оба: `user_id, pred`, **250000 строк**, порядок совпадает с `sample_submit.csv`,
NaN и отрицательных нет. Предсказания сырые, поправка +0.17 не наложена.

**Один сид** (seed 1), как договаривались. Границы совпали с вашей таблицей:
запас по 2 дня от границы обучения, застоялость по 28 дней с обеих сторон.

Оговорка: это сиды **нового** прогона (`--es-metric cal` + когорта трёх блоков),
они сами по себе лучше той версии `tfm3`, что вы уже мерили.

## Направление живое, и заметно живее, чем на вашем бустинге

Мерено против колонки `blend` из `work/preds_pack/` (1.666395), калибровка
поквантильная, 24 бина:

```
                            скор        r      ЗАПАС
свежий, якорь 378        1.668940   0.99631   +0.00217
ЗАСТОЯЛЫЙ, якорь 350     1.725828   0.96132   +0.00424
```

Профиль ровно тот, что вы описывали: скор хуже на 0.057, зато корреляция ошибок
падает с 0.996 до 0.961. **Запас вдвое выше свежего и вдвое выше рекорда проекта
0.00193.** Корреляция застоялого прогноза со свежим 0.956 — это разные объекты,
а не сдвинутая копия.

## Оси складываются. Проверено на нашей паре

Честный OOF: 5 фолдов по юзерам, калибровка учится только на train-фолдах,
усреднено по 3 разбиениям. Библиотека — 30 моделей пакета плюс константа.

```
только пакет                       1.666421
+ свежий tfm2_3                    1.665807    -0.000614
+ застоялый tfm3_stale28           1.666033    -0.000388
+ ОБА                              1.665595    -0.000826

застоялый ПОВЕРХ свежего:                      -0.000212
свежий ПОВЕРХ застоялого:                      -0.000438
```

**Застоялость даёт −0.000212 сверх свежей секвенсной модели** — против ваших
+0.000091 поверх `gseq_small_s42`. В 2.3 раза больше, и это ровно тот эффект,
который вы предсказали: сильной модели не приходится платить качеством за
непохожесть.

Сумма по отдельности 0.001002 против 0.000826 вместе — оси перекрываются
примерно на 18%, то есть независимы не полностью, но в основном.

Все числа выше шумового пола 0.000022: минимальное из них, 0.000212, это
десять шумовых единиц.

## Что дальше с нашей стороны

Раз направление подтвердилось, досчитываем три сида. Нужны два прогона фазы B
по часу (сиды 2 и 3), дальше этап C — минуты. Три сида должны дать ещё немного
за счёт усреднения: на свежих версиях усреднение трёх стоило 0.0003 скора.

Если вам для решения о весах нужны ещё какие-то срезы застоялости — 18 дней
(якорь 360/390) или 8 дней (370/400) — скажите, это тоже только инференс,
по паре минут на срез. Максимум по нашим чекпойнтам — 29 дней, дальше якорь
попадает в обучающий диапазон.
