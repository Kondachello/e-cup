# WF-1: риск-таблица штаба (состояние на 29.08, 21:1x МСК)

Снято фактами с рабочей копии и из git, а не по памяти агентов. Все проверки
перевыполнены заново в этой сессии; где число ниже — оно измерено, а не процитировано.

**Ветка** `sasha`, HEAD = `f166474` («Мёрж origin/olya в sasha: пакет сдачи и ретрейн
заморозки приняты, действующая пара F8+T3 сохранена»), автор Aleksandr Kondakov,
2026-08-29 21:08:40 +0300.
**HEAD запушен**: да — `git status -sb` даёт `## sasha...origin/sasha` без `ahead/behind`,
`git rev-parse HEAD` == `git rev-parse origin/sasha` == `f166474152769d3c1432f563dba6fa33d3e123b9`.
В `main` ничего не уходило.

---

## Таблица рисков

| # | риск | статус | чем закрыт (проверяемый факт) | что осталось |
|---|---|---|---|---|
| 2 | **scores.tsv расходился с реестром / потеря строк на мёрже** | **ЗАКРЫТ** | Файл в индексе чист (`git status --short work/reports/scores.tsv` пуст). **650 строк, 564 ключа** против **632/546** на предмёржевом `8de16ef` — ровно +18 строк и +18 ключей (блок olya: FINAL_PAIR_F5, FINAL_PAIR_SET, MAX_BACK_409, RETRAIN_FREEZE, шесть `lagfz*`, четыре `*_fz`, четыре `*_fz_cal`) плюс наш `ktpp_cal 1.671065`, который присутствует **ровно один раз** (строка 632). Конфликтующих ключей (одно имя — разные числа) было 27 до мёржа и 27 после: **мёрж не внёс ни одного нового расхождения**. | Реестр — журнал с дописыванием: 27 ключей исторически имеют по несколько значений (`blend_opt` — 15). Любой код, читающий scores.tsv по имени, обязан брать **последнюю** строку. Проверить это в потребителях реестра, когда будет слот времени; на пару финалистов не влияет. |
| 4 | **Тяжёлые артефакты попадут в git / раздуют репо** | **ЗАКРЫТ** | Коммит `2b5a138`; `.gitignore` строки 91–93. Проверено `git check-ignore -v`: `ozon_kevf_ckpt.zip` (158 МБ), `sasha2_jf_package.zip` (1.33 ГБ), `work/data/kaggle_session3/` (2.3 ГБ) — все три игнорируются. `__pycache__/` игнорируется правилом строки 20. | Ничего в git. Но см. риск №11 — игнор не есть бэкап. |
| 5 | **kevf3 (joint fusion) без провенанса** | **ЗАКРЫТ** | Коммит `fa30191`: в индексе 8 файлов `work/colab/out/kevf3_*` (два сида × val/test parquet + json + log), плюс `work/reports/kevf3_acceptance_final_2908.md`, `work/handoff/KEVF3_CLOSED_2908.md`, `work/handoff/HANDOFF_zhenya_kevf3_s2024.md`. Вердикт приёмки: максимальный честный вклад **+0.000011** при пороге пары **0.000044**, в наборе с tfm4 вес ровно нулевой — в сборку не идёт. | Мелкий долг по тексту: в §1 приёмки написано «в git НЕ коммичено, только положено» — с момента `fa30191` это неверно, файлы отслеживаются. Поправить фразу при следующей правке отчёта. Ось **не перезапускать**. |
| 6 | **Мёржи zhenya / kostet / olya затрут нашу пару или гард** | **ЗАКРЫТ** | Все три ветки в `git branch -r --merged HEAD` (`c5e357d` zhenya, `9c156cf` kostet, `f166474` olya). Ключевая проверка: `git diff 8de16ef..HEAD -- work/scripts/finalist_guard.py` = **0 строк**, то есть гард байт-в-байт предмёржевой; устаревшие `PRIVATE_EV` и `FINAL_MARKED=(F5_priv, F4_priv)` от olya **не приняты**. Прогон гарда сейчас: «Лучший законный файл: **F8_priv** (1.6458057)», `F5_priv` не всплывает нигде. | Ничего по мёржам. Хвост: в дереве остаются ссылки на мёртвую линию `lbmix2` (`solution_lbmix2/`, `final_submission/README.md`, `PLAN_FINAL_WEEK.md`, `work/zhenya_eda/subs.json` и др.) — это исторический код, не пересекающийся со сборкой пары; чистить не срочно, но в пакете жюри упоминание сверить. |
| 7 | **Доктрина разъехалась с историческими замерами** | **ЗАКРЫТ** | `.venv/bin/python work/doctrine/tests/test_doctrine.py` → **20/20 тестов прошло** (κ из параболы против реестра, mdl_talc, закон проскальзывания на шести точках, дозы, EB-приор, пороги зондов, `w_private`). | `pytest` **не установлен** в `.venv` — документированный в шапке файла запуск `python -m pytest work/doctrine/tests -q` падает с `No module named pytest`. Либо `pip install pytest` в venv, либо переписать шапку на standalone-запуск. Сами законы проверены. |
| 8 | **Пакет сдачи не собирается / потеряны артефакты после мёржей** | **ЗАКРЫТ** | `.venv/bin/python final_submission/inference.py --stage check` → **EXIT=0**, «базовых моделей готово: **18/18** (суммарный вклад 1.006 из 1.006)», «всего не хватает артефактов: **0**». Идентично эталону, снятому до мёржей на `8de16ef`. Единственный `inference.py` репозитория — `/Users/alexanderkondakov/ozon-cup/final_submission/inference.py`. | Напоминание самого чека: перед отгрузкой веса из `work/models` скопировать в `final_submission/models` (раздел «Упаковка» в README). Переобучение на чистой машине — 9.2 ч последовательно, параллель не запускать (16 ГБ, машина уже падала дважды). |
| 9 | **Витрина SHOW11 проскочит в финалисты** | **ЗАКРЫТ** | `work/scripts/finalist_guard.py`: `SHOW_PATTERN = r"^SHOW"`, `BANNED_PATTERNS = (SHOW_PATTERN, r"_maxpub", r"_aggr")`. Прогон печатает «ТРЕВОГА: автоматический выбор возьмёт SHOW11_hull4, SHOW10_l3e3 — из них ЗАПРЕЩЕНЫ: оба» и «ВЫБИРАТЬ ПАРУ РУКАМИ ОБЯЗАТЕЛЬНО». Хэши пары сверены заново: `F8_priv` → `5eaaef9f…f4abf0f`, `T3_g1_redose_044` → `39d5cd24…eb656e72`, по **250001** строке каждый, оба в `submissions/` и в git. E[priv] пары = **1.6460786** (ведёт F8), запас над вторым слотом +0.0014026. | **Бан ^SHOW не снимать.** Исключение в коде (`allow_show_slot2`) требует одновременно слот 2 + коммит-поправку с маркером `ДОКТРИНА-ПОПРАВКА: SHOW-СЛОТ-2`; такого коммита нет и заводить его без решения штаба нельзя. |
| 10 | **Галка слота-1 на платформе стоит не на F8_priv** | **ОТКРЫТ — только руками Саши** | Со стороны репозитория закрыто всё, что можно: файлы, хэши, гард, E[priv]. Папка `work/reports/platform_status_2908/` **создана и в git** (коммит `e729ee8`), в ней `README.md` с чек-листом: `01_finalist_checkboxes.png` (галки + имена файлов), `02_submission_counter.png` (счётчик посылок — весь план трат построен на 6 оставшихся), `03_deadlines.png` (30.08 23:59 заливка / 31.08 12:00 выбор пары), `04_board_top.png`, `05_final_checkboxes_31.png`. | **Скринов в папке нет ни одного** — лежит только README. Саше: зайти на платформу, сверить имена в галках **посимвольно** с `F8_priv` и `T3_g1_redose_044`, при необходимости переставить с `F7_priv`, снять скрин ПОСЛЕ перестановки и положить файлы в папку. Никакой агент это сделать не может. |

---

## Незакоммиченный хвост рабочей копии

`git status --short` даёт три пути (21 файл, суммарно **640 КБ** — всё лёгкое, тяжёлого
среди них нет):

| путь | что это | размер |
|---|---|---|
| `work/reports/lineA/` | линия A: EB-приор и пять состояний GLS (`gls_state_{base,eb,mom,reml,t35}.npz`, `k1b_*_*.json`, `a1_eb_prior.md/json`, `solve_gls_k1b_A1.py`) | 528 КБ |
| `work/reports/rank/` | ранговая задача: `r2_k_eff.json`, `r2_k_eff.md` | 112 КБ |
| `work/scripts/k_eff_machine.py` | машина оценки k_eff под развилку слота-2 | 44 КБ |

Это выработка агентов, работающих прямо сейчас; коммитит **оператор**, не я.
`__pycache__` внутри `lineA/` уже под игнором.

---

## Что штаб должен сделать следующим

1. **Саша, платформа, сейчас** — галки слота-1/слота-2 сверить с `F8_priv` / `T3_g1_redose_044`, скрины положить в `work/reports/platform_status_2908/`. Это единственный риск, который может стоить всей кампании, и единственный, который не закрывается кодом.
2. **Саша, бэкап, сегодня** — `ozon_kevf_ckpt.zip` (+ `sasha2_jf_package.zip`) на внешний носитель.
3. **Оператор** — закоммитить хвост (`work/reports/lineA/`, `work/reports/rank/`, `work/scripts/k_eff_machine.py`) и запушить в `sasha`; после этого рабочая копия снова чистая.
4. **Подтвердить счётчик посылок по скрину** — если оставшихся не 6, план трат на 30–31.08 пересчитывается целиком.
5. **`pip install pytest` в `.venv`** (или правка шапки `test_doctrine.py`), чтобы документированный запуск доктрины не падал у следующего исполнителя.
6. **Мелкий долг** — поправить фразу «в git НЕ коммичено» в `work/reports/kevf3_acceptance_final_2908.md` §1.
7. **Перед отгрузкой** — скопировать веса `work/models` → `final_submission/models` и повторить `inference.py --stage check` уже на упакованном дереве.

**Не делать:** не снимать бан `^SHOW`; не перезапускать kevf3/joint_fusion; не пушить в `main`; не запускать параллельно два тяжёлых обучения.
