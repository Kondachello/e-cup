# Что нужно восстановить (редакция 2, 24.08 вечер)

**Первая редакция этого файла была неверна.** Она утверждала, что
`work_kostya/scripts/` и `work/colab/gpu_seq.py` отсутствуют в git и что 41.2%
веса бленда невоспроизводимо. Проверено по свежим архивам всех веток — **это не
так**. Ошибка была в источнике: снимки веток `_br_*.zip/tgz` весили 1–2 МБ против
130 МБ полного `git archive`, то есть содержали только часть дерева, а копия `.git`
в контейнере была от 20.08, раньше нужных коммитов.

## Факт: по коду воспроизводимо 100% веса бленда

| член | вес | скрипт обучения |
|---|---|---|
| kostya46_cal | 0.246021 | `work_kostya/scripts/cube.py` ✓ |
| fusion_v3c_avg_cal | 0.228925 | `work/scripts/train_fusion3.py` ✓ |
| gseq_small_s42_cal | 0.108701 | `work/colab/gpu_seq.py` ✓ |
| fusion_v3ctl_cal | 0.106124 | `train_fusion3.py --n-ch 8` ✓ |
| wklin | 0.070987 | `train_wklin.py` ✓ |
| weak_an_d_cal | 0.045493 | `train_weak.py --mech anchors` ✓ |
| weak_ft_recency_cal | 0.043429 | `train_weak.py --mech ftype` ✓ |
| behavonly_avg_cal | 0.041373 | `train_behavonly.py` ✓ |
| lagd28 | 0.035049 | `work/scripts/lag_tta.py` ✓ |
| c_ts2_s42_cal | 0.033278 | `train_gbdt.py --objective two_stage` ✓ |
| gseq_big_s42_cal | 0.024431 | `gpu_seq.py arm=big` ✓ |
| fusion_f_cal | 0.010602 | `train_fusion.py` ✓ |
| febspec2_cal | 0.008916 | `train_febspec2.py` ✓ |
| wklin_wk_cal | 0.002788 | `train_wklin.py` ✓ |

`final_submission/models/chain_test.npz` (8 МБ) тоже на месте — я и его объявлял
пропавшим зря.

## Что действительно нужно сделать

1. **Закоммитить актуальный `blend_reopt.json`.** В `main` и в ветке Жени лежит
   версия на **20 членов, вал 1.666302**. Действующий бленд — **14 членов, вал
   1.665647**, он есть только в рабочей копии Саши. Чистый клон сейчас собирает
   не то решение. Это единственная дыра, которая реально ломает воспроизведение.
2. **Добить `work/scripts`.** В `main` 154 файла, у Саши 175 — не хватает 21,
   включая `rebuild_final_chain.py` и `make_r_candidates.py`.
3. **`LICENSE`** — этого действительно нет ни в одной ветке. Одна строка работы.
4. **Привести `final_submission/` в соответствие.** README §4 и
   `reproduce_training.md` описывают бленд из девяти моделей, из которых с
   действующим пересекаются две, и у обеих другой вес (0.316 → 0.0106,
   0.246 → 0.0333). `inference.py` собирает третий, ещё более старый набор.
   Это writing, не вычисления.
5.
   Посмотреть глазами проверяющего по §2.12.

## Чего нет и почему это, скорее всего, нормально

- Весов моделей (`work/models/` в `.gitignore`) и калибровочных таблиц.
- Тензоров `work/seq2` (~11 ГБ) и `work/seq3` (~7 ГБ), матриц `work/features`.
  Пересобираются `build_seq2.py` / `build_seq3.py` / `build_features*.py`.

Если правила требуют воспроизводимый **код** — всё это не нужно. Если требуют
артефакты — надо перечитать формулировку и считать отдельно. `train_fusion3.py`,
кстати, не про GPU: `device = args.device or ("mps" if ... else "cpu")`, ветки
CUDA в нём нет вовсе.

## Один честно невоспроизводимый член

`gseq_big_s42_cal` (0.024431): обучение оборвано на шаге 4500 из 11736, взято
усреднение чекпоинтов. Код есть, но точный повтор невозможен в принципе. Либо
описать как есть, либо выкинуть член из бленда.
