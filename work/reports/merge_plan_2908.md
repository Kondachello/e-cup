# Мёрж-план 29.08: origin/zhenya → origin/kostet → origin/olya в `sasha`

**Статус: СУХОЙ ПРОГОН ВЫПОЛНЕН. Мёрж НЕ сделан, коммитов НЕТ, рабочее дерево не тронуто.**

Порядок: **zhenya → kostet → olya**. Обоснование порядка — в конце (§7).

---

## 0. Резюме за тридцать секунд

| | |
|---|---|
| Текстовых конфликтов (`merge-tree`) | **НЕТ НИ ОДНОГО** на всех трёх шагах |
| Реальная опасность | **молчаливые перезаписи**: `sasha` не трогал 9 файлов с базы, версия olya встанет БЕЗ вопроса |
| Блокеры до старта | **3** грязных файла (`scores.tsv`, `finalist_guard.py`, `finalists.md`) — git откажется мёржить |
| Гейт «pytest work/doctrine» | **невыполним как записан**: pytest в `.venv` не установлен. Замена — §6.1 |
| Гейт «git grep lbmix2 == пусто» | **невыполним как записан**: 14 файлов уже сегодня, каталог `solution_lbmix2/` лежит в репозитории. Замена — §6.3 |
| Пара F8+T3 после всех трёх мёржей | **ГОДНА** (проверено прогоном guard на симулированном дереве), бан `^SHOW` цел |

Главный риск не технический, а смысловой: **olya привозит устаревшую рекомендацию `F5_priv + F4_priv`**
в `finalist_guard.py`, `finalists.md` и `KNOWLEDGE.md`. Если её не погасить — guard после мёржа
печатает «РЕКОМЕНДОВАН В ФИНАЛИСТЫ-1: F5_priv» и прямо утверждает, что за F8 «приват не платит».

---

## 1. Что и как проверялось (метод)

```bash
git fetch --all --prune          # выполнено: обновился только origin/zhenya (3391a48..bd3f448)
```

Конфликты искались **без единой записи в рабочее дерево**:

1. `git merge-tree --write-tree --name-only <ours> <theirs>` — даёт дерево-результат;
   ненулевой выход и список путей означали бы конфликт.
2. Последовательность из трёх мёржей симулирована цепочкой `commit-tree` (объекты в базе,
   **ни одна ссылка не двигалась**):

```
sasha 9971e53 + origin/zhenya  -> дерево 412468e -> коммит 6cbd243
      6cbd243 + origin/kostet  -> дерево 10c170e -> коммит 44c5531
      44c5531 + origin/olya    -> дерево 4dd0fcf -> коммит fc38c73   <- итог
```

3. Гейты прогнаны в **одноразовом worktree** `.../scratchpad/wt` на коммите `fc38c73`;
   worktree удалён (`git worktree remove --force` + `prune`).

Исходное состояние на момент прогона: `sasha` == `origin/sasha` == **9971e53**.

> **Ветка живая.** Пока шёл анализ, другие агенты добавили в `sasha` ещё два коммита
> (`ac371d6` часть M Жене, `e729ee8` папка под скрины). Симуляция **повторена от нового
> HEAD `e729ee8`** — результат тот же: **на всех трёх шагах ноль текстовых конфликтов**.
> Новые коммиты трогают только `work/handoff/*`, которых нет ни в одной из трёх веток.
> Выводы плана от смещения HEAD не зависят, но перед мёржем прогнать перепроверку из §2.

---

## 2. БЛОКЕРЫ — снять ДО первого мёржа

`git merge` откажется работать («Your local changes would be overwritten»), если входящая
ветка меняет файл, изменённый в рабочем дереве. Таких файлов **три**, все привозит olya:

| файл | что локально | что привозит olya | действие |
|---|---|---|---|
| `work/reports/scores.tsv` | +1 строка `ktpp_cal 1.671065` (не в коммите) | +18 строк, **0 удалений** | закоммитить строку ДО мёржа |
| `work/scripts/finalist_guard.py` | идёт переделка под F8+T3 (другой агент) | своя переделка под F5/F4 | дождаться агента, закоммитить, дальше — §5.2, правило «наша версия» |
| `work/reports/finalists.md` | **переписан под F8+T3** (30 упоминаний F8) | своя редакция под F5/F4 | закоммитить, дальше — §5.1, правило «наша версия» |

Остальная грязь (`ans2.md`, два `.zip`, `work/data/*.parquet`, `work/colab/out/*`,
`work/reports/final_pair_calibration.json`) **ни одной веткой не трогается** — мёржу не мешает.

> Рабочее дерево живое: за время прогона `finalists.md` и `finalist_guard.py` были переписаны
> другим агентом прямо в ходе анализа. **Перед стартом перепроверить:**
> ```bash
> cd /Users/alexanderkondakov/ozon-cup
> git status --short
> git fetch --all --prune
> for b in zhenya kostet olya; do
>   echo "== $b"; git merge-tree --write-tree --name-only sasha origin/$b >/dev/null || echo "ПОЯВИЛСЯ КОНФЛИКТ"
> done
> ```

---

## 3. Шаг 1 — `origin/zhenya` (bd3f448)

**База:** 521e3c2 · **приезжает:** 21 коммит, 81 файл, **+7846 / −72**

### Что приезжает
- **`work/doctrine/`** — библиотека законов кампании в коде + `tests/test_doctrine.py` (20 тестов).
- Отчёты `zhenya_F…L`: F (приватная доза, стоп-правило), G (три числа под подпись),
  H (вердикты), I (заморозка, хедж T3), J (ранговая оптимизация), K1/K1b (аудит σ, локальная σ_u),
  **K2 (запечатанный прогноз)**, K3 (монография), K4 (план вскрытия), L (математика TPP),
  **`zhenya_F8_signature.md` — подпись F8** (это последний коммит ветки, ради него мёрж и нужен).
- `work/zhenya_eda/` — 33 скрипта + 21 json выкладок.
- `work/reports/kappa_registry.json` и `work/zhenya_eda/kappa_registry.json` (+426 / +422 строк).
- `README.md` (+72 — навигация по отчётам).

Обратно — тоже.
zhenya регулярно вливал `origin/sasha` в себя (10 merge-коммитов), поэтому дельта чистая.

### Как разрешать
Разрешать нечего — `git merge origin/zhenya` проходит автоматически.

### Гейт после шага 1
```bash
cd /Users/alexanderkondakov/ozon-cup
.venv/bin/python work/doctrine/tests/test_doctrine.py     # ждём: 20/20 тестов прошло
git grep -c "" -- work/reports/zhenya_F8_signature.md     # файл должен существовать
```
**Проверено в симуляции: 20/20 прошло.**

---

## 4. Шаг 2 — `origin/kostet` (3886ecd)

**База:** bebc367 (kostet отстаёт от `sasha` на 181 коммит) · **приезжает:** 1 коммит, 17 файлов, **+948 / −0**

### Что приезжает
Только новый каталог **`work_kostya/`**, ни одного существующего файла не трогает:
- `REPORT_RESEARCH_WEEK.md` — итог недели: ktpp (neural TPP) даёт вклад 0.000072/0.000061 по сидам,
  среднее 0.000075, парный контроль пройден — **не шум**; ftt (attention-over-features) закрыт нулём;
  SSL не запускался; дистилляция ждёт parquet учителя от mdl_wulfen.
- `scripts_tpp/` (6 скриптов), `preds/` (ktpp val/test, ftt val), `artifacts/` (чекпойнты + json).
- **Двоичное:** `ktpp_ckpts.tar.gz` 610 КБ + три parquet ≈ 5.4 МБ. Итого ~6 МБ в историю.

### Конфликты
**НЕТ.** Трёхточечная дельта — чистое добавление (0 удалений, 0 изменений существующих файлов).
Отставание kostet на 181 коммит роли не играет: мёрж трёхсторонний, откатов не будет.

> `scores.tsv` в двухточечном сравнении «отличается», но это лишь отставание ветки;
> в трёхточечной дельте `scores.tsv` **отсутствует** — kostet его не привозит.

### Как разрешать
Разрешать нечего. Отдельно проверить, что `ktpp_cal` из §2 в `scores.tsv` не задвоился.

### Гейт после шага 2
```bash
git status --short | grep -c '^UU'                        # ждём: 0
cut -f1 work/reports/scores.tsv | sort | uniq -d          # ждём: пусто (нет дублей ключей)
.venv/bin/python work/doctrine/tests/test_doctrine.py     # ждём: 20/20 (не сломали шаг 1)
```

---

## 5. Шаг 3 — `origin/olya` (469c325) — **ЗДЕСЬ ВСЯ РАБОТА**

**База:** a806d77 · **приезжает:** 15 коммитов, 14 файлов, **+690 / −138**

### Что приезжает
- **Пакет сдачи** (`final_submission/README.md`, `models/README.md`, `reproduce_training.md`,
  `inference.py`): состав и воспроизводимость приведены к действующему бленду; из пакета убраны
  8 посторонних файлов; риск §2.12 снят (окружение ставится из пакета).
- `inference.py` — правки **чисто языковые** (перевод на язык прод-системы, правило от 28.08):
  «замеренный на лидерборде» → «оценённый для целевого окна». **Функционально ноль изменений.**
- `README.md`, `TEAM_PLAN.md` — одна и та же починка команды установки:
  `-r solution_lbmix2/requirements.txt lifetimes torch` → `-r final_submission/requirements.txt`.
- `work/scripts/lag_tta.py` (фикс коллизии `note`), `mb_fix_infer.py`, `retrain_table*` —
  ретрейн заморозки (7.8% веса переобучено, сдвиг +0.000080), MAX_BACK 379→409.
- `scores.tsv` +18 строк, `KNOWLEDGE.md` +124 строки, `finalists.md` +131 строка,
  `finalist_guard.py` +42 строки — **всё под пару F5_priv + F4_priv**.

### Конфликты: текстовых НЕТ, но есть ДЕВЯТЬ МОЛЧАЛИВЫХ ПЕРЕЗАПИСЕЙ

`sasha` не менял эти файлы с базы a806d77 → git возьмёт версию olya **без вопроса**:

| файл | правило | что делать |
|---|---|---|
| `work/reports/finalists.md` | **НАША версия** | см. 5.1 |
| `work/scripts/finalist_guard.py` | **НАША версия** | см. 5.2 |
| `work/reports/scores.tsv` | **ОБЪЕДИНЯТЬ** | см. 5.3 |
| `work/reports/KNOWLEDGE.md` | **ОБЪЕДИНЯТЬ** | см. 5.3 |
| `final_submission/README.md` | **версия olya** | принять как есть |
| `final_submission/models/README.md` | **версия olya** | принять как есть |
| `final_submission/reproduce_training.md` | **версия olya** | принять как есть |
| `final_submission/inference.py` | **версия olya** | принять; опечатка — 5.4 |
| `README.md`, `TEAM_PLAN.md` | автослияние | проверено, см. 5.5 |

Отдельно: **`work/scripts/predict_lb.py` — правило соблюдается само.**
У `sasha` реестр **MEASURED = 135 записей**, у olya — **123**, и версия olya равна базе.
135 ⊃ 123 (у olya нет ни одной записи, которой нет у нас;
Побеждает наша, больший реестр. **Вмешательство не требуется, но проверить гейтом (§6.4).**

#### 5.1 `finalists.md` — сохранить нашу
**Файл уже переписан** под пару F8+T3 (заголовок «Пара финалистов: F8_priv + T3_g1_redose_044»,
редакция 29.08, 30 упоминаний F8). Правки пока НЕ закоммичены. Значит:

1. **Закоммитить их до мёржа** (иначе git откажется мёржить — §2).
2. После коммита конфликт с olya **гарантирован** (обе стороны переписали шапку файла).
3. Разрешение: `git checkout --ours work/reports/finalists.md`.
4. Затем **руками перенести** из версии olya структурные разделы, которых у нас нет:
   «Правило отбора: не лучший по паблику, а лучший по приватной EV», «MAX_BACK 379→409 —
   невыкупленный остаток», «Чек-лист заморозки».
   Разделы «ПАРА ОБНОВЛЕНА: F5_priv + F4_priv» и «ПАРА ВЫБРАНА И ОТМЕЧЕНА FINAL (F3+F4)» —
   перенести **как ИСТОРИЮ, пометив «отменено 29.08»**; действующей парой остаётся **F8 + T3**.

#### 5.2 `finalist_guard.py` — сохранить нашу, но сверить два инварианта
Сейчас файл переписывается под F8+T3 (уже видно: `SHOW_PATTERN = r"^SHOW"`, `NOISE_PER_DIR`,
`PRIV_TRANSFER`, `--k`). Версия olya переписывает **тот же участок** (`BANNED_PATTERNS`,
`PRIVATE_EV`, `report_auto`) под F5/F4 → **настоящий конфликт гарантирован**.

```bash
git checkout --ours work/scripts/finalist_guard.py
```
Затем проверить два инварианта:
1. **`^SHOW` в бане.** Есть у обеих сторон (`BANNED_PATTERNS = (r"^SHOW", r"_maxpub", r"_aggr")`).
   **Бан не снимать ни при каком разрешении.**
2. **`PRIVATE_EV` / `FINAL_MARKED` не должны остаться от olya.** В её версии зашито
   `FINAL_MARKED = ("F5_priv", "F4_priv")` и `PRIVATE_EV["F5_priv"] = +0.000923`.
   Прогон на симулированном дереве печатает буквально:
   > `РЕКОМЕНДОВАН В ФИНАЛИСТЫ-1 по приватной EV: F5_priv … Это НЕ лидер паблика: F8_priv лучше
   > по публичному скору, но приват за него не платит`

   — прямое противоречие решению 29.08. Наша версия должна это заменить.

При этом сама проверка пары в версии olya работает верно:
`--pair F8_priv.csv T3_g1_redose_044.csv` → **«ВЕРДИКТ: пара годна»**, SHOW11/SHOW10 помечены
ЗАПРЕЩЁН. То есть даже худший исход мёржа пару не ломает, ломает только рекомендацию.

#### 5.3 `scores.tsv` и `KNOWLEDGE.md` — объединение тривиально
Проверено по ключам и по знакам диффа:
- `scores.tsv`: у olya **0 удалений**; множество ключей olya **строго содержит** наше
  (приезжают `FINAL_PAIR_F5`, `FINAL_PAIR_SET`, `MAX_BACK_409`, `RETRAIN_FREEZE`, шесть `lagfz*`,
  `*_fz`, `*_fz_cal` — 18 ключей). **Ни одного нашего замера не теряется.**
- `KNOWLEDGE.md`: у olya **0 удалений**, только 4 новых раздела в конец.

Значит взятие версии olya **и есть** union — при условии, что локальная строка `ktpp_cal`
уже закоммичена (§2). Проверка после мёржа — гейт §6.4.

#### 5.4 `inference.py` — принять, но исправить опечатку
В версии olya на строке ~694 задвоено слово:
`"НЕ ХВАТАЕТ ФАЙЛА: … (оценённая цепочка поправок поправок)"`. Косметика, но текст идёт в пакет.

#### 5.5 `README.md` / `TEAM_PLAN.md` — автослияние проверено и оно ПРАВИЛЬНОЕ
`README.md` меняют **и zhenya, и olya**, `sasha` — нет. Автомёрж дал ровно то, что нужно:
навигация zhenya (232 строки) **плюс** починенная olya строка установки
(`-r final_submission/requirements.txt` вместо `solution_lbmix2/...`). Различие с zhenya —
ровно одна строка 207, различие с olya — блок навигации. Ручного вмешательства не требуется.

### Гейт после шага 3 — полный, см. §6

---

## 6. Гейты — точные команды

Все команды из `/Users/alexanderkondakov/ozon-cup`.

### 6.1 Доктрина — 20/20
Задание просило `pytest work/doctrine`. **Так нельзя: pytest в `.venv` не установлен**
(`No module named pytest`). Рабочая замена — штатный раннер zhenya:
```bash
.venv/bin/python work/doctrine/tests/test_doctrine.py
# ждём последней строкой: 20/20 тестов прошло
```
*(если pytest поставят — `.venv/bin/python -m pytest work/doctrine -q` тоже подойдёт)*

### 6.2 Пакет сдачи — `--stage check`
Правильный путь: **`/Users/alexanderkondakov/ozon-cup/final_submission/inference.py`**
(другого `inference.py` в репозитории нет).
```bash
.venv/bin/python final_submission/inference.py --stage check ; echo "EXIT=$?"
```
**Замер ДО мёржа (эталон): `EXIT=0`, «базовых моделей готово: 18/18», «всего не хватает артефактов: 0».**
После мёржа обязано быть то же самое. Коды: 0 — всё на месте, 1 — не хватает восстановимого,
2 — блокирующее. **Любой EXIT ≠ 0 после мёржа = откат шага 3.**
Стадия только читает, ничего не пишет — запускать безопасно.

### 6.3 lbmix2
Гейт «`git grep lbmix2` пусто» **невыполним и сегодня**: в репозитории лежит сам каталог
`solution_lbmix2/` плюс исторические упоминания в `KNOWLEDGE.md`, `probe_design.md`,
`lb_predictor.md`, `PLAN_FINAL_WEEK.md`, `.gitignore` — **14 файлов до всякого мёржа**.
Смысл гейта — «пакет и инструкции не зависят от старого пакета». Рабочая формулировка:
```bash
# (а) ни одной ЗАВИСИМОСТИ от старого пакета в инструкциях и коде — ждём ПУСТО:
git grep -n "solution_lbmix2/requirements\|solution_lbmix2/scripts\|mix_lbmix2" -- \
    README.md TEAM_PLAN.md final_submission/ work/scripts/

# (б) в исполняемом коде пакета упоминаний нет вообще — ждём ПУСТО:
git grep -nl lbmix2 -- 'final_submission/*.py' work/scripts/
```
**Проверено на симулированном итоговом дереве: (а) и (б) — пусто.** В `final_submission/README.md`
остаются строки 199/203, но это осознанное пояснение olya «`solution_lbmix2/` не используется» —
не зависимость. Если штаб хочет буквально пустой `git grep lbmix2` — это отдельная задача
(удалить каталог `solution_lbmix2/`), **в этот мёрж её не тащить**.

### 6.4 Наши правила не потеряны
```bash
# бан витрины цел:
grep -n 'BANNED_PATTERNS\|SHOW_PATTERN' work/scripts/finalist_guard.py | grep -q '\^SHOW' \
  && echo "OK: ^SHOW в бане" || echo "ТРЕВОГА: бан ^SHOW снят"

# реестр MEASURED не усох (было 135):
sed -n '/^MEASURED/,/^\]/p' work/scripts/predict_lb.py | grep -cE '^\s*\("'   # ждём >= 135

# scores.tsv не потерял ключей и не задвоил:
cut -f1 work/reports/scores.tsv | sort | uniq -d                              # ждём пусто
wc -l < work/reports/scores.tsv                                               # ждём >= 650

# действующая пара цела и годна:
shasum -a 256 submissions/F8_priv.csv submissions/T3_g1_redose_044.csv
# ждём 5eaaef9f… и 39d5cd24…
.venv/bin/python work/scripts/finalist_guard.py --pair \
    submissions/F8_priv.csv submissions/T3_g1_redose_044.csv | tail -5
# ждём: «ВЕРДИКТ: пара годна» И НИ ОДНОГО упоминания F5_priv в рекомендации

# в отчётах действующая пара — F8+T3, а не F5+F4:
grep -c 'F8' work/reports/finalists.md                                        # ждём > 0
```

### 6.5 Санитарный
```bash
git status --short | grep '^UU'          # ждём пусто (неразрешённых конфликтов нет)
git diff --stat HEAD                     # глазами: ничего лишнего не подмешалось
```

---

## 7. Почему такой порядок

- **zhenya первой** — она уже вливала `sasha` в себя, пересечение с нами пустое, мёрж
  бесплатный; после него появляется `work/doctrine/`, то есть **гейт 6.1 начинает работать**
  и страхует два следующих шага.
- **kostet второй** — чистое добавление `work_kostya/`, ничего не может сломать; ставим его
  до olya, чтобы конфликтный шаг был последним и откатывался в одиночку.
- **olya последней** — единственный источник смысловых конфликтов. Если шаг 3 придётся
  откатить (`git merge --abort` или сброс на коммит после шага 2), результаты zhenya и kostet
  останутся в ветке.

---

## 8. Риски

1. **Устаревшая рекомендация F5/F4 переживает мёрж** — главный. Guard начнёт советовать
   `F5_priv` вместо `F8_priv`, а `finalists.md` и `KNOWLEDGE.md` будут утверждать, что пара
   зафиксирована как F5+F4. Цена ошибки — не тот файл в финальном слоте. Лечится §5.1, §5.2, гейт 6.4.
2. **Гонка с другими агентами.** `finalist_guard.py` и `finalists.md` были переписаны прямо
   в ходе этого прогона и **до сих пор не закоммичены**. Мёржить, пока они грязные, нельзя:
   git откажется, а `git stash` рискует потерять свежую редакцию пары. Сначала коммит, потом мёрж.
   Перед стартом — перепроверка из §2.
3. **Два гейта из задания невыполнимы как записаны** (pytest не установлен; `lbmix2` в репозитории
   исторически). Замены в §6.1 и §6.3 — не смягчение, а перевод гейта на то, что он проверяет по смыслу.
4. **~6 МБ двоичного** от kostet в историю (чекпойнты + parquet). Обратимо только переписыванием истории.
5. Проверено явным сравнением списков файлов.

---

## 9. Черновик последовательности (выполнять только после §2)

```bash
cd /Users/alexanderkondakov/ozon-cup
git status --short                                     # должно быть чисто по scores.tsv и finalist_guard.py

git merge origin/zhenya                                # ожидание: без конфликтов
.venv/bin/python work/doctrine/tests/test_doctrine.py  # 20/20

git merge origin/kostet                                # ожидание: без конфликтов
cut -f1 work/reports/scores.tsv | sort | uniq -d       # пусто

git merge origin/olya                                  # ожидание: конфликт по finalist_guard.py И finalists.md
git checkout --ours work/scripts/finalist_guard.py
git checkout --ours work/reports/finalists.md
# далее — ручной перенос разделов по §5.1 и сверка инвариантов по §5.2
# затем гейты §6.1–§6.5 целиком

# git commit НЕ ДЕЛАТЬ без отдельного распоряжения штаба
```

---

*Прогон 29.08. Мёржей не выполнялось, коммитов не создавалось, ссылки не двигались.
Симуляция — объекты `commit-tree` в базе (`fc38c73` и предки) и одноразовый worktree в скретчпаде (удалён).*
