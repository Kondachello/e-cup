# Костя — НОЧЬ 29→30.08: масштаб ktpp (линия C кампании, единственный живой рычаг)

Контекст: дедлайн вернулся — заливка до **30.08 23:59**, посылок у команды осталось
**6 суммарно**, жёсткий стоп новых сборок 30.08 18:00. Пара сейчас F8_priv
(1.6458057389) + T3. Твой ktpp — единственная вал-живая модельная ось недели
(X = 0.000075 подтверждён тройкой сидов, доктрина tabless объяснила почему),
и эта ночь — его единственное окно доехать до финальной сборки.

## Задача ночи (по убыванию приоритета, вердикт по каждому пункту)

1. **Ёмкость**: подними ёмкость энкодера (d/слои) на ступень против текущего
   конфига — ровно один парный контроль (тот же сид, та же валидация gap30).
2. **Сиды**: доведи до 3 сидов лучшей конфигурации, усреднение в лог-пространстве.
3. Калибровочные мишени: модель, правильно
   ловящая промо, должна ВОСПРОИЗВОДИТЬ наши замеренные сегментные сдвиги
   (промо κ=+0.286, наклон по чеку +0.61) на вале без всякого дозирования.
4. Таблицу в модель НЕ добавлять ни в каком виде (правило tabless, три подтверждения).

Ресурс: **карта Жени свободна** (s2024 отменён) — если удобно, раскидай сиды на
две машины; постановка у него занимает 15 минут, он в курсе.

## Выгрузка и дедлайны (жёстко)

- Стандартная тройка на каждый вариант: `ИМЯ_{val,test}.parquet + ИМЯ.json`
  (провенанс: конфиг+сид+команда) — в ветку или любым способом Саше.
- **К 08:00 МСК 30.08** — числа (val-скоры, X по margin, если посчитаешь сам).
- **К 14:00 МСК 30.08** — файлы. Что приедет после 16:00 — в финальную сборку
  физически не попадает.
- Отрицательный вердикт к утру ценнее «почти готово» к вечеру.

## Гейт включения (чтобы ты знал правила игры и не ждал слота)

Приёмка на нашей стороне: калибровка → margin.pair_contribution → joint_gain.
- X < 0.0003 → ось не едет (текущие 0.000075 — мимо, нужен рост в 4 раза).
- X ≥ 0.0003 → считаем corr(d_new, d_N1), где d_N1 — направление уже уплаченного
  зонда mdl_wulfen: corr ≥ 0.7 → апостериор κ̂≈+0.08, не включаем; corr ≤ 0.4 → приор
  свежий, ось едет **слепой дозой 0.309** в сборку 16:00.
- Зонд на LB НЕ ставится ни при каком исходе: у модельной оси q ≈ 10·X, при любом
  достижимом X ценность зонда < 1 шума. Слот тебе не нужен — только файлы.

## Дистилляция — НЕ стартовать

Учитель обновлён: `work/preds/blend_opt_val.parquet` (вал) + `submissions/F8_priv.csv`
(тест, НЕ F6/F7). Но не запускай, пока мы не пришлём подтверждение parquet —
ночь целиком отдай пунктам 1–3.
