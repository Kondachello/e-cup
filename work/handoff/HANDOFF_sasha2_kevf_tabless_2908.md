# Саша-2 — GPU-задание на сегодня: kevf-TABLESS на твоём Kaggle (линия B кампании)

Сначала: kevf3.zip получен, принят, раскладывается в репо — провенанс-долг
GPU-трека закрывается, спасибо. Ось kevf3 закрыта замером (член — шум при отличном
соло; причина — таблица в бленде насыщена, corr 0.998 у любого, кто её видит).

Из этого закрытия родилось новое задание — последний GPU-выстрел до дедлайна
(заливка до **30.08 23:59**, стоп сборок 18:00): тот же kevf, но **строго без
таблицы**. Доктрина tabless: ценность даёт член, который таблицу принципиально
НЕ видит (kevf3 с таблицей = шум; ktpp без таблицы = живой; рекорд запаса —
tfm4_tabless). Ожидание честно скромное (~1 шум) — гоним, потому что квота
простаивает и посылок это не тратит.

## Отличия от твоего kevf3-прогона (три правки, остальное — ровно как было)

1. Датасет `ozon-tabfeats-wed` НЕ подключать (Input: только `ozon-ecup-t3`,
   `ozon-code`, `ozon-kevf-ckpt` с тёплыми базами kevf_v2/kevf_gru). В ячейке 1
   убери ассерты про `tabf16_*` — таблицы в этом прогоне нет.
2. Ячейка 3: **MAX_STEPS = 12000**; из cmd УДАЛИ три пары аргументов:
   `"--tab", ""`, `"--tab-mode", "concat"`, `"--tab-dropout", "0.15"`.
   Не заданный `--tab` = чисто событийный путь, побитово v2 (см. хелп в
   kaggle_seq.py). Имена ног: `kevf_tl_s42` (encoder tfm, база kevf_v2.ckpt)
   и `kevf_tl_gru` (encoder gru, база kevf_gru.ckpt) — та же двухногая схема,
   что у тебя в kevf3.
3. Ячейку 2 (build хранилища) НЕ меняй — гони теми же параметрами, что в
   kevf3-прогоне: тёплые базы уже доказали совместимость с этим хранилищем.
   Контроль tabzero не актуален (коллапса в таблицу без таблицы не бывает) —
   следи только за скором в логе.

Квота: нужно ≥6 ч (батч ~5.5). Если квоты нет — напиши сразу, не начинай:
линия отменяется без последствий.

## Выгрузка и дедлайн

Output версии → `kevf_tl_*_{val,test}.parquet + *.json` (+ckpt себе) — Саше
любым способом **до 12:00–14:00 МСК 30.08**. Позже 16:00 — в финальную сборку
не попадает.

## Что будет с результатом (правила игры)

Приёмка у нас: калибровка → margin.pair_contribution → joint_gain. Зонд/слот на LB
эта линия не просит ни при каком исходе.
