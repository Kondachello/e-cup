# Женя — задание на 27–28.08: теория переноса и приватная доза финала

Контекст: сегодня твоя измерительная машина отработала на полную — и принесла
новые данные, которые требуют именно твоей математики.

соло-замеров членов бленда (посылки теперь БЕЗЛИМИТНЫЕ);
- mdl_talc — зонд пересборки бленда полным шагом, q=0.0249 (!), κ=0.113±0.047 —
  классовый приор R (0.53–0.62) сломался; пятое подтверждение «классам не верить»;
- R8 = доза 0.124 по твоему закону, факт лёг в расчёт с точностью 2.7e-6;
- Z-разложение от базы R8: все поосные |κ| ≤ 0.07 (я применил ПРИОР 0 вместо
  0.333 — для микро-осей разложения плоский приор 0.333 фабрикует дозы); расчёт 1.6463751,
  факт 1.6463210 (−0.5σ) — лучший честный файл, выше старого SHOW-потолка;
- SHOW4 (полный оптимум оболочки, ридж 1e-3): расчёт 1.6442404, факт
  1.6449518 — слип реализации +0.00071 на Σ|c|=37 (курс оптимизатора на
  ошибке аппроксимации mean_P против mean_250k).

## Задачи

1. **Реестр и закон.** Внеси mdl_talc и Z-разложение в kappa_registry/transfer_law
   (точки с алгебраическими κ пометь отдельным классом — у них σ измерения 0,
   вся σ — выборочная паблик→попгенеральная). Обнови вывод по классовым приорам.

2. **Формализуй дозу микро-осей.** Мой выбор «приор 0 для осей разложения,
   0.333 для настоящих новых осей» — импровизация. Нужна твоя версия: какой
   приор корректен для (а) осей-разностей внутри уже собранного оптимума,
   (б) сконструированных направлений (джойнт), (в) новых модельных осей.
   Плюс winner's-curse поправка для направлений, ПОСТРОЕННЫХ по паблик-замерам:
   слип SHOW4 (+0.00071 при Σ|c|=37, ридж 1e-3) — твоя калибровочная точка.

3. **Приватная EV-доза финала.** Главный вопрос перед заморозкой: R9 собран
   под паблик-замеренные κ с усадкой; пересчитай его дозы под максимум
   E[private] с учётом (а) выборочного шума 50k→200k, (б) накопленного налога
   на приват по всем ~35 осям кампании, (в) курса победителя. Выдай: вектор доз
   финалиста-1 (возможно, глобальный множитель s<1 на R9-добавку к T3) и
   ожидаемый интервал привата. Это станет финальной сборкой 29–30.08.

4. **Стоп-правило.** По твоей доктрине: накопленная переоценка паблика > 0.0005
   → стоп жать оболочку. сколько ещё МОЖНО собирать с оболочки,
   если вообще можно. SHOW4–SHOW6 в решение не входят, но их замеры — данные.

Посылки бесплатные: любой контрольный замер для твоих оценок — проси, зальём.
Скрипты: make_axis_probe.py (--joint), predict_lb.py, разложение —
в сессионных заметках Саши, повторяется одной командой от любой базы.
