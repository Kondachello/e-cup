# Саша (kostet) — задание на 27–28.08

Паблик-топ-1
взят арбитражным SHOW4 (1.6449518) — по явному исключению Саши, НЕ финалист.
Посылки на платформу бесплатные. Твой kostya46 в Z-разложении несёт лёгкий
плюс (κ=+0.034) — живой член состава.

## 1. Провенанс-долг внешних моделей (приоритет 1, это блокер жюри)

38.7% веса действующего бленда лежит в репо «числами, а не кодом»: kostya46
0.246, gseq_small 0.109, gseq_big 0.024, fusion_f 0.011 (PLAN_FINAL_WEEK §3).
Твоя часть — kostya46 и всё, что у тебя от gseq:

- код обучения + точные команды/сиды/якоря → `work_kostya/` (проверь, что
  каталог реально в git: `git ls-files work_kostya | head`);
- веса и калибровки боевых kostya46 (s1, avg2) → архив для
  `final_submission/models/` (Оля собирает пакет, её handoff — HANDOFF_olya_2708);
- если ретрейн с текущего кода расходится с отгруженным — не страшно, честная
  планка «Δ измерен» (см. retrain_table.md), но Δ надо померить и записать.

## 2. Фьюжн-ось (приоритет 2, компьют)

Ось «прокачать fusion» не пробована. Задача: расширенный прогон рецепта
fusion_f (train_fusion3.py) — больше сидов/якорей, средний по сидам, калибровка.

- на своей машине — как удобно; на нашей — через очередь
  (`work/scripts/enqueue.py`, приоритеты 850+, ночь у нас занята DART/bagged,
  утро — ретрейном заморозки Оли);
- приёмка: `margin.pair_contribution` ≥ 0.0003 (точная алгебра, не 7.1·запас²);
- test-преды обязательно (`*_test.parquet`) — зонд оси на LB соберём мы, база R9.

## 3. Опционально: резервный GPU-слот

Если у тебя есть свой Kaggle/Colab GPU — скажи Саше: есть готовый пакет сессии 3
(kevf3, work/kaggle/NOTEBOOK_JF.md); третий сид s2024 на твоём аккаунте
диверсифицирует пару, которую он гоняет сейчас (s42/s1337 на своём).
