# Команда — задания на 30.08. ВСЕ НА СКОР ДО ЗАМОРОЗКИ

Сводка Саши, 30.08 ~14:00. Заменяет `TEAM_TODO_2908.md` целиком.

**Дедлайны.** Заливка посылок — **30.08 23:59 МСК**. Жёсткий стоп новых сборок —
**18:00**. Галки — **31.08 12:00 МСК**. Посылок осталось **5**.

**Питч отложен до после заморозки.** До жюри 12.09 двенадцать дней; до 31.08 12:00
любая рука дороже на скоре.

---

## 0. ЧТО ИЗМЕНИЛОСЬ ЗА НОЧЬ И УТРО — прочитать всем

**1. F12_ebint замерен 1.6456761695614883 и стоит в слоте-1.** Печать прогноза
(`f12-seal`, до отправки) давала 1.6456663 — промах **+9.9e-6, это 0.45 шума**, внутри
68%-полосы. Все три условия §9 `finalists.md` выполнены, перестановка штатная.
Δk против F8 ≤ +0.99 при пороге безубытка 3.94, потому что сегментные tau при
расщеплении **сужаются все три**, а расширяется только модельная семья.

**2. Настоящая доска оказалась хуже, чем мы считали.** По скрину 30.08 (308 команд)
впереди нас **пять** команд, а в `p_top3.py` в поле стояли **три**, причём HSE Бобры
устаревшим числом. Поле исправлено:

| | паблик | посылок | E[приват] |
|---|---|---|---|
| Ежи | 1.6443877 | 73 | 1.645377 |
| ICEQ | 1.6445537 | 97 | 1.645553 |
| Daniil Stepanov | 1.6449845 | 96 | **1.645937 ← порог 3-го места** |
| DeepTech AI | 1.6452091 | 75 | 1.646124 |
| T | 1.6457820 | 11 | 1.646107 |
| **мы, F12** | 1.6456762 | 63 | **1.646073** |

Место 1 «итмони» 1.6440063524 — это мы, витрина SHOW11, в финалисты не идёт.

**3. Отсюда честная планка.** `E[C_(3)] = 1.6458054`:

| состояние | P(топ-3) | P(топ-5) |
|---|---|---|
| F8 (как было) | 16.5 % | 48.6 % |
| **F12 (уже сделано)** | **26.5 %** | **64.9 %** |
| + слот-2 F8 | 26.9 % | 65.4 % |

Отставание от третьего места — **12.2 шума**. **Тройка придёт из дисперсии, а не из
ожидания.** Потолок — примерно один шанс из трёх, и это после того, как всё бесплатное
забрано. Никто не обещает больше. (Строка «+ бестабличная ось ~33 %» из первой редакции
СНЯТА — см. 0-БИС.)

---

## 0-БИС. ОТМЕНА ЭТАПА «БЕСТАБЛИЧНАЯ ОСЬ» (обновление ~14:40)

**Бестабличная ось УЖЕ ПРИМЕНЕНА. Посылки на неё не тратим.** Это отменяет пункт из
первой редакции этой сводки и мою же утреннюю запись в KNOWLEDGE.

Ошибка была методическая: `joint_gain` меряет прирост к колонке `blend` пакета, то
есть к **неисправленной** базе. Отправляемый файл — это бленд + 46 осей поправок, и
они уже несут то же направление. Прямая проверка (`work/scripts/axis_applied.py`):

| файл | проекция на бестабличную ось |
|---|---|
| **F12_ebint** | **1.071 шага** |
| F8_priv | 1.060 |
| T3 | 1.066 |

`cos = +0.582`; случайное направление той же нормы даёт 0.000 ± 0.0035 — наблюдаемое
на **307 сигм** от случайного, совпадение исключено. Поодиночке: `tfm4_tabless` 1.001,
`tfm4` 0.819, `kevf_tl_gru` 0.334, `ktpp` −2.576 (цепочка активно идёт ПРОТИВ ktpp —
это согласуется с зондом mdl_wulfen, где κ = −0.240).

Цена непроверенной гипотезы: по `doctrine/probes.probe_value` ось «стоила» бы 4.1 шума
= **+8.1 п.п. P(топ-3)** и прошла бы любой гейт. Настоящая цена ортогонального
остатка — **0.03 шума = +0.06 п.п.**

**Новое правило приёмки: перед тем как считать модель осью, гнать
`work/scripts/axis_applied.py --model ИМЯ`.** `joint_gain` против пакета — верхняя
граница, а не оценка; он не знает про цепочку.

## 0-ТРИ. ЧТО ОСТАЛОСЬ ПОСЛЕ ЭТОГО

**ПОПРАВКА САШИ (15:00): несожжённый слот стоит НЕ ноль, а минус.** Правило «слот
стоит ноль» писалось, когда у слота было будущее применение. У дедлайна опционная
стоимость нулевая, а зонд **не трогает пару**: файл, который не лёг лучше, просто не
получает галку. Значит гейт «ΔP ≥ 1 п.п.» к зондам неприменим — тратить надо всё,
что имеет ненулевую информационную ценность. Отсюда 0-ЧЕТЫРЕ — главное задание дня.

Осталось бесплатное:

1. **Слот-2 → F8_priv.** Перебраны ВСЕ замеренные файлы как слот-2 при F12 в слоте-1:

   | слот-2 | ΔP(топ-3) | ΔP(топ-5) |
   |---|---|---|
   | **F8_priv** | **+0.40** | **+0.54** |
   | F7_priv | +0.09 | +0.12 |
   | T3, F5, F6, F4, F3 | +0.00 | +0.00 |

   F8 лучший по ОБОИМ критериям — прежняя дилемма «топ-3 против E[priv]» тут не
   возникает. (Оговорка: k для F7/F6/F5/F4/F3 — оценки, в реестре только F8 и T3.
   Вывод от них не зависит: F8 выигрывает тем, что ближе всех к F12, откл 0.2 шума.)

2. **Дисперсия — посчитана, и это РАЗВИЛКА, а не рычаг.** Мы на 8.8 шума хуже точки
   смены знака (mu = 1.645880, там P(топ-3) = 44.4 % — сходится с `objective.md`).
   Значит для топ-3 дисперсия союзник: `dP/dsigma = +0.645 п.п. на шум`.
   **Но для топ-5 она враг:**

   | sigma | P(топ-3) | P(топ-5) |
   |---|---|---|
   | 1.0x | 26.51 % | 64.85 % |
   | 2.0x | 33.90 % | 58.14 % |

   Удвоение сигмы покупает +7.4 п.п. топ-3 и продаёт −6.7 п.п. топ-5. **А в топ-5
   решает жюри.** Это выбор цели, а не расчёт, и делает его Саша.


---

## 0-ЧЕТЫРЕ. ГЛАВНОЕ ЗАДАНИЕ ДНЯ: собрать и замерить γ=0, Λ×0

**Раз E-канал исчерпан, лучшим оставшимся ходом становится нарочито развязанный
второй файл.** `slot2_lottery.md` §4.2 его отверг с формулировкой «тот же слот,
потраченный на E-фронтовый файл, покупает 8–11 п.п., а лучший билет — 4–7». Сегодня
E-фронтового файла больше нет (0-БИС), и сравнение переворачивается.

Пересчёт на НАСТОЯЩЕЙ доске, пара с F12 в слоте-1:

| пара | P(топ-3) | P(топ-5) |
|---|---|---|
| F12 + F8_priv *(текущий план)* | 26.91 % | 65.39 % |
| **F12 + γ=0, Λ×0** | **32.64 %** | **71.85 %** |
| F12 + γ=0 | 31.33 % | 70.67 % |
| F12 + γ=0 −0.020·v1 | 31.04 % | 69.72 % |
| F12 + F9_eb08 (γ=0.08) | 27.23 % | 65.85 % |

**+5.7 п.п. топ-3 и +6.5 п.п. топ-5 — лучше по обоим.** Механизм: у γ=0 почти то же
ожидание, что у F12 (хуже всего на 0.6 шума), но своя дозовая sd 8.41 шума против 4.60.
В зачёте по `min(g1,g2)` независимая добавка дисперсии при том же среднем — это и есть
настоящий лотерейный билет.

### Риск назван прямо

γ=0 — это отказ от усадки, то есть полное доверие замеренным κ. Линия A против:
LOO по группам пережили две из пяти, F9 при полном OOF −8.56 шума, а
`gamma_for_top3.md` даёт для γ=0 при полном OOF **3.32 %** против 20.91 %. То есть
если κ переоценены, файл не «чуть хуже», а провальный.

**Поэтому протокол — печать и замер, а не вера.** Собрать → запечатать прогноз
паблика в git → замерить (1 посылка) → **галку ставить ТОЛЬКО если замер лёг в
коридор** `|замер − алгебра| ≤ 1.2e-4`. Промах = мы узнали, что κ переоценены, и
остаёмся на F12+F8, потратив слот, который всё равно сгорал. Ровно так прошёл F12:
печать 1.6456663, факт 1.6456762, промах 0.45 шума.

### Кто и что. СРОК — сборка до 18:00, замер до 21:00

Материалы **уже есть**: `work/reports/lineA/gls_state_a3g0.npz` содержит все 46 осей,
матрицы `Q`, `V`, `Lam`, `cQ`, `cP`, дозы `d_fin` при γ=0, база F7_priv
(F0 = 1.6458557351). По `a3_gamma_path.json` прогон солвера в точке γ=0 уже делался и
сошёлся с паблик-алгеброй до 7-го знака.

**Блокер — направления.** На диске в `gls_draft/directions/` лежат 32 из 46.
**НЕ ХВАТАЕТ 14: seg_cinnab, seg_corund, seg_vivian, seg_verdit, seg_felsit, seg_silica, seg_amber, seg_lapis, seg_kyanit, seg_dolom, seg_indigo, seg_larvik, seg_basalt, seg_gneiss.**
У кого есть `work/features/` и CSV зондов seg_cinnab–seg_gneiss — тот и собирает; у меня их нет.

> **СНЯТО 30.08 ~16:45.** Восстанавливать 14 направлений не нужно: файл γ=0 был
> посчитан ещё 29.08, его вектор lp лежал в скретчпаде той сессии (путь записан в
> самом рецепте). Собран `submissions/F13_g0.csv`, печать прогноза —
> `work/reports/lineA/SEAL_F13_g0.md`, разбор и карта решения —
> `work/reports/lineA/SLOT2_g0_decision.md`. **Оля свободна.**
> Поправка к арифметике этого раздела: выигрыш не +5.7 п.п., а **+1.4 п.п.** —
> у линии γ=0 k = 11.27, а не 9.3 (расширенный tau_model без расщепления
> сегментного приора). Вывод «γ=0 — лучший второй слот» сохраняется.

**Оля** — это твоя машинка (гранд-пересборка F5 по 138 направлениям) и твой приоритет
№1, выше чистой комнаты. Порядок:
1. восстановить 14 недостающих направлений (`d = lp(зонд) − lp(база)`);
2. собрать γ=0 и γ=0,Λ×0 из `gls_state_a3g0.npz`;
3. посчитать паблик-алгебру, **запечатать прогноз коммитом**, отдать Саше на замер.

**Женя** — до сборки: подтверди или опровергни цифры выше на своём поле, и главное —
**дай коридор прогноза для γ=0**, чтобы было с чем сравнивать замер. Это твой K1b/V_star.

**Если 14 направлений восстановить не успеваем** — берём F8 вторым слотом (+0.40 п.п.)
и не рискуем. Это дефолт, и он остаётся в силе до тех пор, пока γ=0 не ЗАМЕРЕН.


---

## Женя — поле и дисперсия. ТЫ БЛОКИРУЕШЬ ОСТАЛЬНЫХ, начни с п.1

1. **Пересчитать M4 целиком.** Он написан против F8 и против поля из трёх команд —
   оба входа устарели. `BOARD` в `work/scripts/p_top3.py` я уже исправил по скрину
   (27 соперников, `E[C_(3)] = 1.6458054`, сверь). Нужны новые P(1)/P(топ-3)/P(топ-5)
   и новые разрывы под F12. **Твои прежние 20.8 % не воспроизводятся ни на старом
   коде (93 %), ни на новом поле (26.5 %) — разберись, на каком поле они считались.**

2. **ДИСПЕРСИЯ КАК РЫЧАГ — главное.** `objective.md` показал `dP/dsigma > 0`, пока
   P(топ-3) < 44 %. Мы на 26.5 %, то есть **вне денег: расширение хвоста покупает п.п.**
   Это единственный рычаг, **не требующий ни одной посылки**, и на настоящем поле его
   никто не считал. Нужен ответ числом:
   - какая пара файлов максимизирует P(топ-3) при нашем mu;
   - где по mu производная меняет знак на новом поле (на старом было 1.645965);
   Часть этого я уже посчитал (0-ТРИ п.2): знак меняется при mu = 1.645880, мы хуже
   на 8.8 шума, `dP/dsigma = +0.645 п.п. на шум`. **Проверь меня и ответь на главное:
   стоит ли +7.4 п.п. топ-3 продажи −6.7 п.п. топ-5, если в топ-5 решает жюри.**
   И есть ли вообще законный способ поднять сигму, не ухудшая mu.

3. **Контрподписать Δk(F12)** через `V_star`, маршрут C `k_eff_f8_countersign.py`.
   Мой счёт дифференциальный (route D): Δk ≤ +0.99 при пороге 3.94. **Абсолютное k у
   меня не сошлось с твоим аудитом — 18.4 против 8.3**, набор осей в реконструкции
   неполон. Закрой своим маршрутом.

4. **K2 — печать v3** под фактическую пару. Коммит = печать, ДО вскрытия.

## Оля — СКОР, не пакет и не питч

1. **Чистая комната по F12 — жёстко сегодня.** Он слот-1, а протокол по нему не
   прогонялся ни разу (делался для F5/F7). То, что есть — `assemble_finals.py` +
   манифест — это пересборка в **том же** окружении, и её собственный докстринг
   предупреждает, что sha256 сходится «только на той же версии polars», то есть
   решающей не является. **Это единственное, что может обесценить слот-1 целиком.**
   Свежий клон → сборка из сырья по гроссбуху → побайтовая сверка + sha256 против
   того, что реально загружено на платформу.

2. **MAX_BACK 379→409 — выкупить остаток.** Твой же незакрытый пункт (`finalists.md`
   П2): исправленные признаки тестового якоря собраны (`anchor=2026-02-13.mb409.*`,
   три тира), сдвиг замерен (по 0.093 покрытого веса среднее −0.000744, sd 0.003719,
   у 2.26 % юзеров >0.01), но остаток **осознанно не выкуплен**. Это не новая гипотеза,
   а известная поправка признаков. Посчитай, что она даёт в п.п. P(топ-3) — и если
   положительно, внеси в сборку.

3. **СНЯТО: GLS-пересборка с бестабличной осью.** Ось уже применена (0-БИС), пересборка
   под неё ничего не даёт. **Вместо этого:** прогони `axis_applied.py` по всем моделям
   пакета (27 колонок) и найди хоть одну с проекцией ≈ 0. Это поиск живой оси без
   единой посылки, и он твой по машинке. Нашлась — сразу ко мне, до 17:00.

4. **Ревизия молчащих.** Твоя находка: «молчание недодозировано, цепочка разъела дозу»,
   seg_realgr лёг в 0.1σ от линии полного переноса. В F8/F12 суммарная `mdl_olivin` = 1.672.
   Проверь, не остался ли хвост под новым (расщеплённым) приором F12 — приор silent
   уехал с +0.012 на −0.2032, это ровно твоя зона.

## Костя — GPU/семейства

**ПЕРЕНАПРАВЛЕНО (0-БИС): маржинал ktpp над набором больше не нужен — набор применён.**
Проекция ktpp = −2.576: цепочка идёт ПРОТИВ него, и это согласуется с твоим же зондом
mdl_wulfen (κ = −0.240). Семейство закрыто, и закрыто честно.

**Новое задание: `axis_applied.py` по всем твоим моделям** — ktpp всех сидов, ftt, что
осталось от SSL. Ищем проекцию ≈ 0 при заметном q: это была бы живая ось, и на неё
посылка есть. До 17:00. Ниже — прежний текст, оставлен для истории.

1. ~~Маржинал ktpp поверх набора~~ `{tfm4, tfm4_tabless, kevf_tl_gru}`. Соло-гейт
   0.0003 ты не взял (0.000161), и по правилу хендоффа ось не едет. **Но в наборе она
   не выдавливается:** `joint_gain` даёт вес 0.110 и маржинал **+0.000064** — это
   ~+1.8 п.п. P(топ-3). Померь именно маржинал над набором, не соло. Подтвердится —
   ktpp входит третьим в направление зонда.
   ```
   .venv/bin/python work/scripts/joint_gain.py tfm4 tfm4_tabless kevf_tl_gru
   .venv/bin/python work/scripts/joint_gain.py tfm4 tfm4_tabless kevf_tl_gru ktpp
   ```
2. **Выгрузи недостающие тест-сиды ktpp (2 и 3).** `live_axes.md` фиксирует, что
   тест-сид 2 не выгружен, из-за чего вклад считался по одному сиду.

## Исполнитель kevf3 (Kaggle T4)

**СНЯТО (0-БИС): сиды бестабличного семейства больше не нужны** — ось применена на
1.07 шага, и более точная её версия покупает доли шума. Извини, это моя ошибка в
первой редакции: я оценил ось по `joint_gain` против пакета, не проверив проекцию.

**Если есть силы и желание** — единственное, что даёт НОВОЕ направление, это
SSL-претрейн на неразмеченных последовательностях (в `CONTEXT.md` он «не окупается»,
но это оценка, а не замер). Обязательно бестабличный. К 18:00 не успеть, так что это
задел на питч-демо, а не на посылку. Прежний текст ниже.

~~Ещё 2 сида `kevf_tl_gru`~~, конфиг из приёмки, 65 мин каждый:
```
kevf_tl_gru <- kevf_gru.ckpt
  --d 256 --layers 6 --heads 8 --ff 512 --lmax 320 --encoder gru
  --batch 384 --lr 2e-4 --epochs 2 --steps 12000 --anchors 24
```
Усреднение сидов — единственный безотказный источник в проекте (+0.0002…0.0005 на
семейство), и оно прямо поднимает X бестабличной оси. **Нужно к ~17:00**, чтобы попасть
в урожай. Усреднять в log1p. `kevf_tl_s42` не трогать — он выброшен.

## Костя Мортиков (трек 5, RTX 2060) — ты без задания с 28.08

**СНЯТО (0-БИС): бестабличные сиды больше не нужны.** Если берёшься — только прогон из
`CONTEXT.md` (`--es-metric cal` + когорта трёх блоков + переобучение на train+val):
это НЕ та же ось, там меняется процедура отбора якорей, а по доктрине штаба платят
именно свойства постановки, а не модели. Проверять результат через `axis_applied.py`,
а не через `joint_gain`.

## Саша

Галки (решение по слоту-2), зонд, урожай, заморозка, пуш.

---

## Правила, которые не меняются

- **Прогноз паблика коммитится ДО отправки посылки.** sha256 рядом.
- Приёмка: калибровка → `margin.py` → `joint_gain.py`. Сырой вал вердиктом не считается.
- Зонды на LB — только через Сашу.
- Урожай заменяет F12 в слоте-1 только по §9: замерен, лучше по паблику, и
  `|замер − алгебра| ≤ 1.2e-4`.
- **Правило траты посылки переписано.** Старое «ΔP(1) ≥ 3 п.п.» механически
  неисполнимо (P(1) ≈ 1 %, даже 10 шума двигают его на +1.8 п.п.) — это уже
  зафиксировано в `gates/SLOT_DECISION_2908.md` §8.2. **Новое: тратим, если
  ΔP(топ-3) ≥ 1 п.п.**
- Бан `^SHOW` в силе. Ранговый блок k_eff мёртв (фальсификация провалена), развилка
  слота-2 с витриной закрыта.
- **F9_eb08 не отправляем:** его собственная печать заканчивается выводом «слот на F9
  тратить нельзя» (при полном OOF −8.56 шума). Рекомендация GAMMA +1.3 п.п. перекрыта
  более поздним LOO-результатом A5.
