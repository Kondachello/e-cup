# Задания по трекам — 27.08, вторая половина дня (после Части G и сборки F1)

Общий контекст: действующая пара финалистов **F1_priv = 1.6467037592** (замерен,
состав в `work/reports/f1_priv_assembly.md`) + **T3 = 1.6469322** (страховка);
guard пройден. Витрина SHOW8 = 1.6447341 — не финалист. Стоп-дата новых
осей — 29.08; заливка до 30.08 23:59; выбор пары руками до 31.08 12:00.

## Трек теории переноса (законы, дозы)

1. **Сверка перед заморозкой (приоритет 0):** мой ответ на F/G —
   `work/reports/f_reply_sasha_2708.md` (ветка sasha): точные 20 пар Z-разложения,
   τ_z = 0.0148 [0; Следствие:
   s\* = 0.75 к R9-добавке даёт привату −0.00017, вместо неё в F1 вошёл
   приватно-оптимальный микровектор (+0.00004). Подтверди или опровергни —
   до 29.08, состав F1 ещё можно менять.
2. Его доза
   0.138 в F1 отдельная. Внеси в реестр.
3. **Слип-закон:** после замера SHOW9/10 кривая станет пятиточечной
   (Σ|c| = 7, 19, 37, 146, 279). Финализируй закон (наклон ~1.75e-5·Σ|c| по трём
   точкам) — это материал и для дозирования, и для питча.
4.
   Два свободных зонда в резерве — скажи, какой разрез нужнее.
5. **Финальная пара:** прогони F1+T3 через E[min] с σ_d-законом трека 4
   (σ_d = 0.0011·rms). Если есть пара лучше — предложи до 29.08.

## Трек 4 (измерения, финалисты, упаковка)

1. **finalists.md:** финалист-1 = F1_priv (1.6467037592), финалист-2 = T3. Задокументируй исключение R9 (приватный мираж, τ_z) и всех
   SHOW (арбитраж).
2. **Ретрейн заморозки:** машина свободна, очередь пуста — ставь свои 800+
   (включая MAX_BACK 379→409). Таблица «отгружено/ретрейн/Δ» — в пакет.
3. **Пакет:** pack_track5.tgz от трека 5 закоммичен; веса kostya46/gseq — за
   треком внешних моделей (напомни им, если не пришли). Финальные числа README —
   после стоп-даты 29.08, не раньше.

## Трек 5 (GPU-секвенсы)

1. Ночь дала законный ноль (три конфига tfm4, максимум +1.2e-5 при пороге 5e-5) —
   направление закрыто честно, спасибо.
2. **GPU свободен — возьми третий сид kevf3:** код `work/kaggle/kaggle_seq.py`
   (v3, обычный torch/cuda, на твоей машине пойдёт), тёплые базы —
   `ozon_kevf_ckpt.zip` в корне репо (kevf_v2/kevf_gru + json), команды — в
   `work/kaggle/NOTEBOOK_JF.md` ячейка 3 (замени find1 на локальные пути,
   сид 2024, --max-steps 12000). Вернуть стандартную тройку
   `kevf3_s2024_{val,test}.parquet + .json`.
3. Если по упаковке трека 5 что-то осталось несданное — сегодня-завтра.

## Трек внешних моделей (kostya46/gseq/fusion)

1. **kevf3 полный прогон:** статус? Пакет `sasha2_jf_package.zip` (1.3 ГБ) +
   `ozon_kevf_ckpt.zip` (165 МБ) должны были дойти. Сколько GPU-часов, когда
   Save & Run All, когда ждать `results_jf.zip`? Если квоты нет — скажи сразу,
   перекинем на трек 5.
2. **Провенанс kostya46/gseq** (веса+код+команды) → пакет трека 4. Это блокер жюри.
3. Ответь, чтобы не дублировать.

## Всем

Приёмка любых новых моделей: калибровка → margin.pair_contribution (порог пары
0.000044) → предложение оси координатору. Зонды теперь через бюджет — сами не
заливаем. После 29.08 — только сборка, подтверждения и питч-материалы.
