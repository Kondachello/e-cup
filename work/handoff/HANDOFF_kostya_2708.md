# Костя — задание на ночь 26→27.08 и день 27.08

Контекст на вечер 26.08: твоя T-ось снята полностью (T3 = 1.6469322, урожай
собран, спасибо за трек 5). κ=0.113) и собрали её урожай: **лучший честный
файл теперь R9_zharv2 = 1.6463210**. Разрыв до топ-1 паблика ~0.0013, и он
добывается только новыми осями — у тебя единственная живая GPU-машина с тёплыми
чекпойнтами tfm4. Файлы SHOW4–SHOW6 в таблице — паблик-арбитраж по разрешению
Саши, НЕ финалисты и не ориентир.

## Задание 1 (ночь, готово к запуску, ничего писать не надо)

Ночной прогон трёх конфигов tfm4 — всё подготовлено ещё 25.08 и не запущено:

- скрипт: `work/scripts/seq/night.sh`, инструкция `work/scripts/seq/NIGHT_RUN.md`
- три конфига вместо трёх сидов (a_s17 / a_s23 / a_s777), ~5ч45м суммарно
- тёплый старт с твоих `model_tfm2_s{1,2,3}.pt` (они только у тебя)
- ожидание +0.0002…0.0004 val к паре tfm4; порог приёмки вклада **0.00005**
  (margin.pair_contribution — точная алгебра, НЕ 7.1·запас²; см. INGEST_FORMULA
  в KNOWLEDGE.md — старая формула занижала вклад сильных моделей в 20 раз)

Утром выгрузи стандартную тройку на каждый конфиг (`ИМЯ_val.parquet`,
`ИМЯ_test.parquet`, `ИМЯ.json`) через `_to_kosta` как обычно. Приёмку и зонд
оси на LB делаем мы — посылки теперь бесплатные, каждая ось получит свой замер.

## Задание 2 (день 27.08, УСЛОВНОЕ — жди отмашки)

m²-BCE-μ²: взвешивание buy-головы весами (μ̂+1)², μ̂ — предсказание величины
чека (обе стороны, не только позитивы; нормировка на среднее=1). На табличных
моделях вариант с таргет-весами провалился (ломает калибровку p), вариант с μ̂²
сейчас на парном замере у нас. ЕСЛИ вердикт положительный (сообщим утром) —
патч в `work/scripts/seq/tfm4.py` (голова `W['buy']`, сборка весов на ~349,
лосс ~626): вес строки = detach(μ̂ из mu-головы), домножить построчный BCE.
Один конфиг, пара с контролем на том же сиде. Без отмашки не жги GPU.

## Задание 3 (параллельно, 20 минут)

Для упаковки (Оля собирает пакет жюри): заархивируй и передай runlogs/ и
чекпойнты боевых tfm4/tfm4_tabless (веса, json конфигов, команды запуска).
Побитовость не нужна — нужна демонстрация «код порождает модели решения».
