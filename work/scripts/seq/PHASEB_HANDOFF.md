# Хэндофф GPU-фазы (для Кости М.) — после фиксов 21.08

Три бага, из-за которых `run_all.py` был запрещён к запуску, починены в ветке
`sasha` (коммит «секвенсный конвейер: три фикса, разблокирующие фазу B»):

1. `train_tcn.py`: OneCycleLR теперь строится ПОСЛЕ подмены `a.steps` на
   `--fixed-steps` — фаза B больше не обрывает отжиг на LR≈5e-4 из пика 7e-4.
   Все прежние прогоны фазы B систематически недоотожжены — их тестовые
   предсказания хуже парных валидационных, ретрейн обязателен.
2. `train_tcn.py`: `return` при `--cal-fixed` больше не падает NameError после
   записи артефактов.
3. `run_all.py`: карантин негодного `*_rt_val.parquet` (`.LEAKY_DO_NOT_USE`)
   выполняется при любом коде выхода и при пропуске по готовому test-parquet.
4. `sweep.py`: base-Namespace дополнен `max_tr_anchor/fixed_steps/cal_fixed/
   cohort1` — испытания больше не пишут nan через общий except.

## Что прогнать (по убыванию ценности)

### 1. tfm3 с честным тестовым зазором (~3 ч GPU)

Сейчас тестовые предсказания tfm3 даёт модель, обученная по якорь 348 —
зазор на тесте 60 дней вместо 30, и +0.000443 вклада — оценка сверху.
Те же три сида, обучение по 378:

```
--arch transformer --minutes 55 --lr 7e-4 --channels 192 --layers 4 --heads 4
--dropout 0.0 --wd 0.0122 --aux 0.25 --batch 512 --ema 0.995 --min-anchor 30
--val-users 0 --val-anchor 408 --seed 1|2|3 --export work/preds --predict ...
```

и пересборка усреднения как в `HANDOFF_tfm3.md`. Вернуть:
`tfm3b_{val,test}.parquet` (контракт exp_lib, 250k строк, порядок sample_submit).

### 2. Фаза B tfm2 (ретрейн по 378) с тремя улучшениями

`run_all.py` теперь можно запускать. Улучшения, которые в LB-результат
1.6488111 ещё не входили: ранняя остановка по калиброванному скору
(`--es-metric cal`), когорта-3, ретрейн по 378 с правильным отжигом (фикс №1).
Центрированную добавку класть с масштабом 0.52 (оптимум параболы по трём
точкам, см. KNOWLEDGE «масштаб исчерпан»).

## Правила, введённые после ваших прогонов (обязательные)

- **Разброс файла**: `sd(log1p(прогноз))` проверять ПОСЛЕ всех шагов сборки,
  а не после приведения к моментам. Канон: **1.631108**. Поправка на молчащих
  раздувала разброс на +0.91%, и полгода никто не видел (это стоило 0.000125).
- **Побитовые дубли**: прежде чем считать два набора разными моделями, сверять
  sha256 ОТДЕЛЬНО на валидации и ОТДЕЛЬНО на тесте (вторая фаза fusion давала
  пять групп с одинаковым тестом при разной валидации).
- **Приёмка**: только прямой замер (`margin.py` — точная алгебра пары,
  `joint_gain.py` — наборы). Формулы 7.1·з² больше нет. Эталон — колонка
  `blend` пакета `work/preds_pack` (после пересборки 21.08 это 1.666302,
  всегда читать из файла, не из чужих отчётов).
- Чекпоинты моделей сохранять (`model_io.save_torch`) — из чистого клона
  сейчас не восстанавливаются пять fusion и wklin (0.397 веса бленда), не
  повторяйте эту дыру.
